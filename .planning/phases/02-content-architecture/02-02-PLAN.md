---
phase: 02-content-architecture
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/sanity/schemaTypes/index.ts
  - src/sanity/schemaTypes/homepageType.ts
  - src/sanity/schemaTypes/siteSettingsType.ts
  - src/sanity/schemaTypes/serviceCategoryType.ts
  - src/sanity/schemaTypes/serviceType.ts
  - src/sanity/schemaTypes/labTestType.ts
  - src/sanity/desk/structure.ts
  - sanity.config.ts
autonomous: true
requirements:
  - CMS-02
  - CMS-03
  - CMS-04
  - CMS-05
  - CMS-09
  - CMS-13

must_haves:
  truths:
    - "Homepage singleton schema contains fields for hero, services highlight, lab tests highlight, and CTA sections"
    - "SiteSettings singleton contains logo, navigation links, contact info, social links, and footer content"
    - "Service documents have name, description, icon/image, category reference, and order field"
    - "LabTest documents have name, description, price, original price, discount, and illustration"
    - "ServiceCategory documents have name, emoji/icon, and order field"
    - "All Studio labels, field descriptions, and group names are in Hungarian"
    - "Studio sidebar groups documents by section: Kezdolap, Szolgaltatasok, Beallitasok"
  artifacts:
    - path: "src/sanity/schemaTypes/homepageType.ts"
      provides: "Homepage singleton document type"
      contains: "defineType"
    - path: "src/sanity/schemaTypes/siteSettingsType.ts"
      provides: "SiteSettings singleton document type"
      contains: "defineType"
    - path: "src/sanity/schemaTypes/serviceType.ts"
      provides: "Service document type with category reference"
      contains: "defineType"
    - path: "src/sanity/schemaTypes/labTestType.ts"
      provides: "LabTest document type with pricing"
      contains: "defineType"
    - path: "src/sanity/schemaTypes/serviceCategoryType.ts"
      provides: "ServiceCategory document type"
      contains: "defineType"
    - path: "src/sanity/desk/structure.ts"
      provides: "Custom Studio desk structure with Hungarian-labeled sidebar groups"
      contains: "structureTool"
  key_links:
    - from: "src/sanity/schemaTypes/serviceType.ts"
      to: "src/sanity/schemaTypes/serviceCategoryType.ts"
      via: "reference field linking service to category"
      pattern: "type.*reference.*to.*serviceCategory"
    - from: "src/sanity/desk/structure.ts"
      to: "sanity.config.ts"
      via: "structureTool receives custom structure"
      pattern: "structureTool.*structure"
    - from: "src/sanity/schemaTypes/index.ts"
      to: "all schema type files"
      via: "imports and re-exports all types in array"
      pattern: "import.*Type"
---

<objective>
Create all homepage, site settings, and services/lab-test related Sanity document schemas with Hungarian labels, plus configure the Studio desk structure with sidebar grouping by page section.

Purpose: Define the content structure that editors will use to manage homepage content, services, lab tests, site settings, and navigation. All fields must be editable per CMS-13.
Output: 5 document type schemas registered in Sanity Studio with a custom sidebar structure in Hungarian.
</objective>

<execution_context>
@C:/Users/stick/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/stick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-architecture/02-CONTEXT.md
@.planning/phases/02-content-architecture/02-01-SUMMARY.md
@sanity.config.ts
@src/sanity/schemaTypes/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Homepage singleton, SiteSettings singleton, and ServiceCategory schemas</name>
  <files>
    src/sanity/schemaTypes/homepageType.ts
    src/sanity/schemaTypes/siteSettingsType.ts
    src/sanity/schemaTypes/serviceCategoryType.ts
  </files>
  <action>
All schemas use `defineType` and `defineField` from 'sanity'. ALL titles, descriptions, and group labels MUST be in Hungarian (per locked decision). Use `validation: (rule) => rule.required()` for essential fields.

**homepageType.ts** — Singleton document. Fixed section order (per decision: no page-builder pattern). Use fieldsets or groups to organize by section:

```
name: 'homepage'
title: 'Kezdolap' (Hungarian for Homepage)
type: 'document'
```

Fields (organized by section):

Hero section fields:
- `heroHeadline` (string, title: 'Fo cim', description: 'A hero resz nagy cimsora', required, max 100 chars)
- `heroSubtitle` (text, title: 'Alcim', description: 'A fo cim alatti leiro szoveg', rows: 3)
- `heroDoctorImage` (image, title: 'Orvos kep', with hotspot: true)
- `heroCards` (array of objects, title: 'Szolgaltatas kartyak', max 4 items). Each card object has:
  - `title` (string, required)
  - `subtitle` (string)
  - `icon` (image)
  NOTE: Card colors are hardcoded in code per decision — NOT in schema

Services highlight section:
- `servicesHeadline` (string, title: 'Szolgaltatasok cim')
- `servicesSubtitle` (text, title: 'Szolgaltatasok leiras')

Lab tests highlight section:
- `labTestsHeadline` (string, title: 'Laborvizsgalatok cim')
- `labTestsSubtitle` (text, title: 'Laborvizsgalatok leiras')

CTA / general:
- `ctaHeadline` (string, title: 'CTA cim')
- `ctaDescription` (text, title: 'CTA leiras')

Use the singleton pattern: add `__experimental_actions` or rely on desk structure to show only one instance. Modern Sanity v5 approach: just use desk structure to filter to a single document with a known `_id`.

**siteSettingsType.ts** — Singleton document:

```
name: 'siteSettings'
title: 'Beallitasok' (Hungarian for Settings)
type: 'document'
```

Fields:
- `logo` (image, title: 'Logo', required)
- `clinicName` (string, title: 'Rendelo neve', required)
- `phone` (string, title: 'Telefonszam')
- `email` (string, title: 'Email cim')
- `address` (text, title: 'Cim', rows: 2)
- `navigationLinks` (array of objects, title: 'Navigacios linkek'). Each link:
  - `label` (string, required)
  - `href` (string, required)
- `socialLinks` (array of objects, title: 'Kozossegi media linkek'). Each social link:
  - `platform` (string, options: list of ['facebook', 'instagram', 'linkedin', 'youtube', 'tiktok'])
  - `url` (url)
- `footerColumns` (array of objects, title: 'Lablec oszlopok'). Each column:
  - `heading` (string)
  - `links` (array of objects with `label` string and `href` string)
- `privacyPolicyUrl` (string, title: 'Adatvedelmi iranyelv URL')
- `metaDescription` (text, title: 'Meta leiras', description: 'Alapertelmezett meta leiras az oldalhoz', rows: 3)

**serviceCategoryType.ts** — Regular document:

```
name: 'serviceCategory'
title: 'Szolgaltatas kategoria'
type: 'document'
```

Fields:
- `name` (string, title: 'Kategoria neve', required)
- `emoji` (string, title: 'Emoji/ikon', description: 'Emoji karakter a szuro gombhoz, pl. "🏥"')
- `order` (number, title: 'Sorrend', description: 'Kisebb szam = elobb jelenik meg')

Preview: Show name with emoji prefix.

Register all three types in `src/sanity/schemaTypes/index.ts` by importing and adding to the schemaTypes array.
  </action>
  <verify>
1. `npm run build` passes
2. `npx tsc --noEmit` passes — all schema types are valid TypeScript
3. Verify each file exports a `defineType(...)` call
4. Verify `src/sanity/schemaTypes/index.ts` imports and includes all three types
  </verify>
  <done>
Homepage singleton, SiteSettings singleton, and ServiceCategory schemas defined with Hungarian labels. All fields individually editable. Registered in schema types array.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Service and LabTest schemas, configure Studio desk structure with Hungarian sidebar</name>
  <files>
    src/sanity/schemaTypes/serviceType.ts
    src/sanity/schemaTypes/labTestType.ts
    src/sanity/schemaTypes/index.ts
    src/sanity/desk/structure.ts
    sanity.config.ts
  </files>
  <action>
**serviceType.ts** — Regular document:

```
name: 'service'
title: 'Szolgaltatas'
type: 'document'
```

Fields:
- `name` (string, title: 'Szolgaltatas neve', required)
- `description` (text, title: 'Leiras', rows: 3)
- `icon` (image, title: 'Ikon/kep', with hotspot: true)
- `category` (reference to serviceCategory, title: 'Kategoria')
- `order` (number, title: 'Sorrend', description: 'Kisebb szam = elobb jelenik meg')

Preview: Show name as title, category name as subtitle (use `select` + `prepare` in preview to dereference category.name). Show icon thumbnail if available.

**labTestType.ts** — Regular document:

```
name: 'labTest'
title: 'Laborvizsgalat'
type: 'document'
```

Fields:
- `name` (string, title: 'Vizsgalat neve', required)
- `description` (text, title: 'Leiras', rows: 3)
- `price` (number, title: 'Kedvezmenyes ar (Ft)', description: 'Az aktualis ar forintban')
- `originalPrice` (number, title: 'Eredeti ar (Ft)', description: 'A kedvezmeny elotti ar — athuzva jelenik meg')
- `discount` (number, title: 'Kedvezmeny (%)', description: 'Kedvezmeny szazalekban, pl. 80')
- `illustration` (image, title: 'Illusztracio', with hotspot: true)
- `order` (number, title: 'Sorrend')

Preview: Show name as title, price formatted as subtitle.

**Update index.ts** — Add serviceType and labTestType to the imports and schemaTypes array (keep existing imports from Task 1).

**structure.ts** — Create `src/sanity/desk/structure.ts` with custom desk structure. This defines the Studio sidebar with Hungarian group labels per locked decision:

```ts
import type { StructureResolver } from 'sanity/structure'

export const structure: StructureResolver = (S) =>
  S.list()
    .title('Tartalom')
    .items([
      // Kezdolap group
      S.listItem()
        .title('Kezdolap')
        .icon(/* HomeIcon or similar */)
        .child(
          S.document()
            .schemaType('homepage')
            .documentId('homepage')
            .title('Kezdolap szerkesztese')
        ),

      S.divider(),

      // Szolgaltatasok group
      S.listItem()
        .title('Szolgaltatasok')
        .child(
          S.list()
            .title('Szolgaltatasok')
            .items([
              S.listItem()
                .title('Szolgaltatasok')
                .schemaType('service')
                .child(S.documentTypeList('service').title('Szolgaltatasok')),
              S.listItem()
                .title('Kategoriak')
                .schemaType('serviceCategory')
                .child(S.documentTypeList('serviceCategory').title('Kategoriak')),
              S.listItem()
                .title('Laborvizsgalatok')
                .schemaType('labTest')
                .child(S.documentTypeList('labTest').title('Laborvizsgalatok')),
            ])
        ),

      S.divider(),

      // Beallitasok group (singletons)
      S.listItem()
        .title('Beallitasok')
        .child(
          S.document()
            .schemaType('siteSettings')
            .documentId('siteSettings')
            .title('Weboldal beallitasok')
        ),
    ])
```

Blog group will be added in Plan 02-03 when blog schemas are defined.

**Update sanity.config.ts** — Replace the plain `structureTool()` with `structureTool({ structure })` importing the custom structure.

NOTE: Use `lucide-react` icons or Sanity's built-in icons for sidebar items. If icons cause import issues, use simple emoji or omit icons — they are not critical.
  </action>
  <verify>
1. `npm run build` passes
2. `npx tsc --noEmit` passes
3. `npx biome check .` passes (add new files to biome scope if needed)
4. Verify `sanity.config.ts` imports and uses custom structure
5. Verify `src/sanity/schemaTypes/index.ts` includes all 5 schema types
  </verify>
  <done>
Service and LabTest schemas defined with Hungarian labels. Studio desk structure configured with sidebar groups: Kezdolap (Homepage singleton), Szolgaltatasok (Services, Categories, Lab Tests), Beallitasok (Site Settings singleton). All list items have rich previews.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `npx tsc --noEmit` passes
3. `npx biome check .` passes
4. 5 schema types registered in schemaTypes/index.ts: homepage, siteSettings, serviceCategory, service, labTest
5. Studio desk structure has 3 sidebar groups with Hungarian labels
6. Homepage and SiteSettings use singleton pattern (fixed documentId)
7. Service has a reference field to ServiceCategory
8. LabTest has price, originalPrice, and discount fields
</verification>

<success_criteria>
- All 5 schema type files export valid Sanity document definitions
- Every field title and description is in Hungarian
- Studio sidebar groups content by page section in Hungarian
- Homepage and SiteSettings singletons use fixed documentId
- Service references ServiceCategory
- All list previews configured (thumbnails + subtitles)
- Build compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-architecture/02-02-SUMMARY.md`
</output>
