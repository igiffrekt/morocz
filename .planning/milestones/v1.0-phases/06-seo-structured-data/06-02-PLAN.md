---
phase: 06-seo-structured-data
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/blog/[slug]/page.tsx
autonomous: true
requirements:
  - SEO-02
  - SEO-09

must_haves:
  truths:
    - "Every page has og:title, og:description, and og:image meta tags"
    - "Homepage uses its own metaDescription and ogImage from Sanity when present, falls back to SiteSettings defaults"
    - "Blog post pages use their own metaDescription and ogImage fields from Sanity, falling back to SiteSettings defaults"
    - "All meta content is in Hungarian"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root metadata with OG defaults from Sanity SiteSettings"
      contains: "openGraph"
    - path: "src/app/page.tsx"
      provides: "Homepage-specific metadata with OG overrides from homepage Sanity data"
      contains: "generateMetadata"
    - path: "src/app/blog/[slug]/page.tsx"
      provides: "Blog post metadata with OG tags from blog post Sanity data"
      contains: "openGraph"
  key_links:
    - from: "src/app/layout.tsx"
      to: "siteSettingsQuery"
      via: "sanityFetch for metadata defaults"
      pattern: "siteName.*defaultOgImage"
    - from: "src/app/page.tsx"
      to: "homepageQuery"
      via: "sanityFetch for homepage-specific metadata"
      pattern: "generateMetadata"
---

<objective>
Wire meta tags and Open Graph data from Sanity into all pages using Next.js Metadata API.

Purpose: Every page gets correct og:title, og:description, og:image populated from Sanity — visible when URLs are pasted into social media preview tools.
Output: Updated layout.tsx with root metadata defaults, page.tsx with homepage-specific metadata, blog/[slug]/page.tsx with enhanced OG metadata.
</objective>

<execution_context>
@C:/Users/stick/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/stick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-seo-structured-data/06-CONTEXT.md
@.planning/phases/06-seo-structured-data/06-01-SUMMARY.md
@src/app/layout.tsx
@src/app/page.tsx
@src/app/blog/[slug]/page.tsx
@src/sanity/lib/queries.ts
@src/sanity/lib/image.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set root metadata defaults from Sanity SiteSettings in layout.tsx</name>
  <files>src/app/layout.tsx</files>
  <action>
    Replace the static `export const metadata` with a dynamic `generateMetadata()` async function in layout.tsx.

    The function should:
    1. Fetch siteSettings via sanityFetch (already done in the component body — but metadata generation is separate in Next.js, so a separate fetch is needed in generateMetadata; Next.js deduplicates the fetch)
    2. Build metadata object:
       - `title.default`: `settings?.siteName ?? settings?.clinicName ?? "Morocz Medical"`
       - `title.template`: `"%s | ${settings?.siteName ?? settings?.clinicName ?? 'Morocz Medical'}"`
       - `description`: `settings?.metaDescription ?? "Morocz Medical — Egeszsegugyi szolgaltatasok"`
       - `metadataBase`: `new URL("https://moroczmedical.hu")` (placeholder — will be updated with real domain)
       - `openGraph.type`: "website"
       - `openGraph.locale`: "hu_HU"
       - `openGraph.siteName`: `settings?.siteName ?? settings?.clinicName ?? "Morocz Medical"`
       - `openGraph.title`: same as title.default
       - `openGraph.description`: same as description
       - `openGraph.images`: if `settings?.defaultOgImage?.asset` exists, use `urlFor(settings.defaultOgImage).width(1200).height(630).url()` — otherwise omit
       - `twitter.card`: "summary_large_image"
       - `alternates.canonical`: "/" (relative — metadataBase handles the domain)
       - `robots.index`: true
       - `robots.follow`: true

    All text in Hungarian. The `lang="hu"` is already on the `<html>` element.

    IMPORTANT: Do NOT change the RootLayout component body or its existing siteSettings fetch — only change how metadata is exported (from static const to async generateMetadata function).
  </action>
  <verify>
    Run `npm run build` to confirm metadata generation works. Check the built HTML output for og: meta tags.
  </verify>
  <done>
    layout.tsx exports generateMetadata() that fetches SiteSettings and returns metadata with og:title, og:description, og:image, og:locale, twitter card defaults. Static metadata const is removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add homepage-specific metadata and enhance blog post OG tags</name>
  <files>
    src/app/page.tsx
    src/app/blog/[slug]/page.tsx
  </files>
  <action>
    **page.tsx (homepage)** — Add `generateMetadata()` async function:
    1. Fetch homepage data (and siteSettings for fallback) using sanityFetch
    2. Return metadata:
       - `title`: override to remove template suffix (homepage should just be the site name)
       - `description`: `homepage?.metaDescription ?? settings?.metaDescription ?? undefined`
       - `openGraph.title`: site name
       - `openGraph.description`: same as description
       - `openGraph.images`: if `homepage?.ogImage?.asset` exists, use that; else if `settings?.defaultOgImage?.asset` exists, use that; else omit
       - `openGraph.url`: "/"
       - `alternates.canonical`: "/"

    **blog/[slug]/page.tsx** — Enhance existing `generateMetadata()`:
    1. The function already fetches the post — additionally fetch siteSettings for OG fallbacks
    2. Add to the returned metadata object:
       - `openGraph.type`: "article"
       - `openGraph.title`: post title
       - `openGraph.description`: `post?.metaDescription ?? post?.excerpt ?? undefined`
       - `openGraph.images`: if `post?.ogImage?.asset` exists, use `urlFor(post.ogImage).width(1200).height(630).url()`; else if `post?.featuredImage?.asset` exists, use `urlFor(post.featuredImage).width(1200).height(630).url()`; else fetch settings and use defaultOgImage; else omit
       - `openGraph.publishedTime`: post?.publishedAt
       - `openGraph.locale`: "hu_HU"
       - `twitter.card`: "summary_large_image"
       - `alternates.canonical`: `/blog/${slug}`

    Import urlFor from "@/sanity/lib/image" in page.tsx (already imported in blog/[slug]/page.tsx).
    All meta text in Hungarian.
  </action>
  <verify>
    Run `npm run build` to confirm both generateMetadata functions compile and resolve. Run `npx biome check .` for lint compliance.
  </verify>
  <done>
    Homepage has its own metadata with OG tags falling back to SiteSettings. Blog post pages have enhanced OG metadata including og:type article, published time, and image cascade (ogImage > featuredImage > defaultOgImage). All meta content is Hungarian. Build passes.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes without errors
- `npx biome check .` reports zero errors
- layout.tsx uses generateMetadata() instead of static const
- page.tsx has generateMetadata() with OG tags
- blog/[slug]/page.tsx has enhanced generateMetadata() with article type and image cascade
- All og:locale set to "hu_HU"
</verification>

<success_criteria>
Every page in the site has og:title, og:description, og:image, and og:locale meta tags populated from Sanity data with appropriate fallback chains. Social media preview tools will show correct Hungarian metadata when any page URL is shared.
</success_criteria>

<output>
After completion, create `.planning/phases/06-seo-structured-data/06-02-SUMMARY.md`
</output>
