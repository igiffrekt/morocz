---
phase: 05-testimonials-blog
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - src/app/page.tsx
autonomous: false
requirements:
  - TEST-01
  - TEST-05
  - BLOG-01
  - BLOG-07

must_haves:
  truths:
    - "Visitor sees testimonial carousel on the homepage below lab tests section"
    - "Visitor sees 3 blog cards on the homepage below testimonials section"
    - "All testimonial and blog content comes from Sanity CMS"
    - "Clicking a blog card navigates to the full blog post detail page"
    - "Blog post detail page renders complete Portable Text content"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage with TestimonialsSection and BlogSection wired to Sanity data"
      contains: "TestimonialsSection"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/sanity/lib/queries.ts"
      via: "allTestimonialsQuery and latestBlogPostsQuery fetches"
      pattern: "allTestimonialsQuery|latestBlogPostsQuery"
    - from: "src/app/page.tsx"
      to: "src/components/sections/TestimonialsSection.tsx"
      via: "renders testimonials carousel"
      pattern: "TestimonialsSection"
    - from: "src/app/page.tsx"
      to: "src/components/sections/BlogSection.tsx"
      via: "renders blog cards"
      pattern: "BlogSection"
---

<objective>
Wire TestimonialsSection and BlogSection into the homepage, adding Sanity queries for testimonials and latest blog posts to the existing Promise.all data fetch. Seed CMS with sample data and verify the complete experience visually.

Purpose: Integrates all Phase 5 components into the live homepage, making testimonials and blog content visible to visitors.
Output: Updated page.tsx with all sections rendered; visual verification of the complete flow.
</objective>

<execution_context>
@C:/Users/stick/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/stick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-testimonials-blog/05-CONTEXT.md
@.planning/phases/05-testimonials-blog/05-01-SUMMARY.md
@.planning/phases/05-testimonials-blog/05-02-SUMMARY.md
@.planning/phases/05-testimonials-blog/05-03-SUMMARY.md
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire TestimonialsSection and BlogSection into homepage page.tsx</name>
  <files>src/app/page.tsx</files>
  <action>
    Update `src/app/page.tsx` to add testimonial and blog data fetching and section rendering.

    1. Add imports:
    ```typescript
    import { TestimonialsSection } from "@/components/sections/TestimonialsSection";
    import { BlogSection } from "@/components/sections/BlogSection";
    import { allTestimonialsQuery, latestBlogPostsQuery } from "@/sanity/lib/queries";
    import type { TestimonialQueryResult, BlogPostQueryResult } from "../../sanity.types";
    ```

    2. Expand the `Promise.all` from 5 queries to 7 queries. Add:
    ```typescript
    sanityFetch<TestimonialQueryResult[]>({ query: allTestimonialsQuery, tags: ["testimonial"] }),
    sanityFetch<BlogPostQueryResult[]>({ query: latestBlogPostsQuery, tags: ["blogPost"] }),
    ```
    Destructure as `[homepage, settings, categories, services, labTests, testimonials, latestPosts]`.

    3. Add sections to the JSX, after LabTestsSection and before the closing fragment:
    ```tsx
    <TestimonialsSection
      heading={homepage?.testimonialsHeadline}
      testimonials={testimonials ?? []}
    />
    <BlogSection
      heading={homepage?.blogHeadline}
      posts={latestPosts ?? []}
    />
    ```

    Section order on homepage: Hero → HeroServiceCards → Services → LabTests → Testimonials → Blog.
  </action>
  <verify>Run `npx biome check .` — zero errors. Run `npx tsc --noEmit` — zero type errors. Run `npm run build` — succeeds. Start dev server with `npm run dev` and verify homepage loads without console errors.</verify>
  <done>page.tsx fetches testimonials and latest blog posts in parallel with existing queries and renders TestimonialsSection and BlogSection below the lab tests section.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of testimonials carousel and blog section</name>
  <files>src/app/page.tsx, src/components/sections/TestimonialsSection.tsx, src/components/sections/BlogSection.tsx, src/app/blog/[slug]/page.tsx</files>
  <action>
Human visual verification of the entire Phase 5 output. Start the dev server with `npm run dev` and open the homepage.

What was built: The homepage now includes a testimonial carousel with fade transitions, dot navigation, swipe gestures, and keyboard accessibility on a teal/green background, plus a blog section showing 3 most recent posts with featured images, titles, and excerpts. Blog post detail pages at /blog/[slug] render full Portable Text content with breadcrumbs and related posts.

Before verifying, seed sample data in Sanity Studio at http://localhost:3000/studio:
- Create 3-4 testimonials with patient names, quote text, and optionally a photo
- Create 2-3 blog categories (e.g., "Egeszseges eletmod", "Labortesztek")
- Create 3-4 blog posts with titles, slugs, excerpts, featured images, and some Portable Text body content (headings, paragraphs, lists)
- Set the homepage "Velemenyek cim" field to "Velemenyek" and "Blog cim" field to "Blog"

Verify the following:

**Testimonials section checks:**
1. Scroll down past Lab Tests — teal/green background section visible
2. One testimonial shown at a time (photo, name, quote)
3. Click dots to navigate between testimonials
4. Use keyboard arrow keys to navigate (click the carousel area first to focus it)
5. On mobile/touch: swipe left/right to navigate
6. Testimonial without a photo should show a placeholder avatar

**Blog section checks:**
7. 3 blog cards visible in a grid below testimonials
8. Each card shows featured image, title, and excerpt
9. No category tags on cards
10. "Osszes blog bejegyzes" link is visible

**Blog detail page checks:**
11. Click a blog card title — navigates to /blog/[slug]
12. Breadcrumb trail at top: Home > Blog > Post Title
13. Featured image displayed prominently
14. Full Portable Text body content (headings, paragraphs, lists)
15. No author or date displayed
16. 2-3 related posts at the bottom

**Responsive checks:**
17. Resize browser to ~375px — testimonials stack, dots visible, blog cards stack
  </action>
  <verify>All 17 visual checks pass. Carousel navigates via dots, swipe, and keyboard. Blog cards link to detail pages. Detail pages render Portable Text correctly.</verify>
  <done>Phase 5 is visually verified: testimonial carousel and blog section display correctly on homepage, blog detail pages render full content with breadcrumbs and related posts, responsive layout works at all breakpoints.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds without errors
- Homepage renders all 6 sections in order: Hero, HeroServiceCards, Services, LabTests, Testimonials, Blog
- Testimonial carousel navigates via dots, swipe, and keyboard
- Blog cards link to /blog/[slug] detail pages
- Blog detail pages are statically generated (no 404s for seeded posts)
- All content comes from Sanity CMS
</verification>

<success_criteria>
A visitor can scroll the homepage and see testimonials in a carousel with smooth navigation, then browse blog posts and click through to full article pages with rich content rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/05-testimonials-blog/05-04-SUMMARY.md`
</output>
