---
phase: 06-seo-structured-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sanity/schemaTypes/siteSettingsType.ts
  - src/sanity/schemaTypes/homepageType.ts
  - src/sanity/schemaTypes/privacyPolicyType.ts
  - src/sanity/schemaTypes/index.ts
  - src/sanity/desk/structure.ts
  - src/sanity/lib/queries.ts
  - sanity.types.ts
autonomous: true
requirements:
  - SEO-02
  - SEO-06
  - SEO-09

must_haves:
  truths:
    - "SiteSettings in Sanity Studio has fields for defaultOgImage and siteName"
    - "Homepage in Sanity Studio has fields for metaDescription and ogImage"
    - "A privacyPolicy singleton document exists in Sanity Studio with Portable Text body"
    - "GROQ queries project the new SEO fields"
  artifacts:
    - path: "src/sanity/schemaTypes/privacyPolicyType.ts"
      provides: "Privacy policy Sanity document schema"
      contains: "privacyPolicy"
    - path: "src/sanity/schemaTypes/siteSettingsType.ts"
      provides: "Updated SiteSettings with defaultOgImage, siteName"
      contains: "defaultOgImage"
    - path: "src/sanity/schemaTypes/homepageType.ts"
      provides: "Updated Homepage with metaDescription, ogImage"
      contains: "metaDescription"
    - path: "src/sanity/lib/queries.ts"
      provides: "Updated queries projecting new SEO fields + privacy policy query"
      contains: "privacyPolicyQuery"
  key_links:
    - from: "src/sanity/schemaTypes/index.ts"
      to: "src/sanity/schemaTypes/privacyPolicyType.ts"
      via: "import and registration in schemaTypes array"
      pattern: "privacyPolicyType"
    - from: "src/sanity/desk/structure.ts"
      to: "privacyPolicy schema"
      via: "singleton desk entry"
      pattern: "privacyPolicy"
---

<objective>
Add SEO-related CMS fields and the privacy policy schema to Sanity.

Purpose: Establish the data layer needed for meta tags, OG images, and the privacy policy page — all editable from Sanity Studio.
Output: Updated SiteSettings (defaultOgImage, siteName), updated Homepage (metaDescription, ogImage), new privacyPolicy singleton schema, updated GROQ queries, updated TypeScript types.
</objective>

<execution_context>
@C:/Users/stick/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/stick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-seo-structured-data/06-CONTEXT.md
@src/sanity/schemaTypes/siteSettingsType.ts
@src/sanity/schemaTypes/homepageType.ts
@src/sanity/schemaTypes/index.ts
@src/sanity/desk/structure.ts
@src/sanity/lib/queries.ts
@sanity.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SEO fields to SiteSettings and Homepage schemas + create privacyPolicy schema</name>
  <files>
    src/sanity/schemaTypes/siteSettingsType.ts
    src/sanity/schemaTypes/homepageType.ts
    src/sanity/schemaTypes/privacyPolicyType.ts
    src/sanity/schemaTypes/index.ts
    src/sanity/desk/structure.ts
  </files>
  <action>
    **siteSettingsType.ts** — Add these fields (in an "SEO" fieldset/group for clarity):
    - `siteName` (string, title: "Weboldal neve", description: "Megjelenik az Open Graph címkékben")
    - `defaultOgImage` (image, title: "Alapértelmezett OG kép", description: "Közösségi média megosztáshoz használt alapértelmezett kép")
    - The existing `metaDescription` field is already present — keep it as-is (it serves as defaultMetaDescription)

    **homepageType.ts** — Add at the end of the fields array:
    - `metaDescription` (text, rows: 2, title: "Meta leírás", description: "Kezdőlap SEO leírása keresőmotorokhoz")
    - `ogImage` (image, title: "OG kép", description: "Közösségi média megosztáshoz használt kép a kezdőlapon")

    **privacyPolicyType.ts** — Create new singleton schema:
    - name: "privacyPolicy", title: "Adatkezelési tájékoztató"
    - type: "document"
    - Fields:
      - `title` (string, required, title: "Oldal cím", default suggestion: "Adatkezelési tájékoztató")
      - `body` (array of block — same Portable Text config as blogPostType: h2/h3/h4/blockquote styles, strong/em/underline decorators, link annotation, bullet/number lists, inline images with alt+caption)
      - `lastUpdated` (date, title: "Utolsó frissítés dátuma")
    - preview: prepare() returns { title: "Adatkezelési tájékoztató" }

    **index.ts** — Import privacyPolicyType and add to schemaTypes array.

    **structure.ts** — Add privacyPolicy singleton under Beállítások group or as a new top-level item with divider:
    - Add a new section above or alongside Beállítások:
      ```
      S.listItem()
        .title("Adatkezelési tájékoztató")
        .child(
          S.document()
            .schemaType("privacyPolicy")
            .documentId("privacyPolicy")
            .title("Adatkezelési tájékoztató szerkesztése")
        )
      ```
    - Place it before the Beállítások divider.

    All field titles and descriptions MUST be in Hungarian. Follow existing schema patterns (defineField, defineType imports from "sanity").
  </action>
  <verify>
    Run `npm run build` to confirm TypeScript compilation succeeds with the new schema fields. Run `npx biome check .` to verify no lint errors.
  </verify>
  <done>
    SiteSettings schema has siteName and defaultOgImage fields. Homepage schema has metaDescription and ogImage fields. privacyPolicyType is registered in schemaTypes and appears in desk structure as a singleton. Build passes without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update GROQ queries and TypeScript types for new SEO fields</name>
  <files>
    src/sanity/lib/queries.ts
    sanity.types.ts
  </files>
  <action>
    **queries.ts** — Update existing queries and add new query:

    1. Update `siteSettingsQuery` projection to include `siteName` and `defaultOgImage` fields.
    2. Update `homepageQuery` projection to include `metaDescription` and `ogImage` fields.
    3. Add new `privacyPolicyQuery`:
       ```
       export const privacyPolicyQuery = defineQuery(`*[_type == "privacyPolicy" && _id == "privacyPolicy"][0]{
         title,
         body,
         lastUpdated
       }`);
       ```

    **sanity.types.ts** — Update hand-authored types:

    1. Update `SiteSettings` type to include:
       - `siteName?: string`
       - `defaultOgImage?: SanityImageObject`

    2. Update `Homepage` type to include:
       - `metaDescription?: string`
       - `ogImage?: SanityImageObject`

    3. Add new `PrivacyPolicy` type:
       ```typescript
       export type PrivacyPolicy = {
         _type: "privacyPolicy";
         _id: string;
         title?: string;
         body?: PortableTextBlock[];
         lastUpdated?: string;
       };
       ```
       (PortableTextBlock should already be imported/defined — check existing types file for the pattern used for blogPost body)

    Follow existing patterns: defineQuery() import from "next-sanity", revalidation tag comments above each query.
  </action>
  <verify>
    Run `npm run build` to confirm all types resolve correctly. Run `npx biome check .` for lint compliance.
  </verify>
  <done>
    siteSettingsQuery projects siteName and defaultOgImage. homepageQuery projects metaDescription and ogImage. privacyPolicyQuery exists with title/body/lastUpdated projection. All corresponding TypeScript types updated. Build passes.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes without TypeScript errors
- `npx biome check .` reports zero errors
- siteSettingsQuery includes siteName, defaultOgImage in projection
- homepageQuery includes metaDescription, ogImage in projection
- privacyPolicyQuery exists and is exported
- privacyPolicyType is in schemaTypes array and desk structure
</verification>

<success_criteria>
The Sanity data layer is ready for SEO work: SiteSettings has SEO fields, Homepage has SEO fields, a privacyPolicy singleton document type exists, all GROQ queries project the new fields, and TypeScript types are updated. Build passes clean.
</success_criteria>

<output>
After completion, create `.planning/phases/06-seo-structured-data/06-01-SUMMARY.md`
</output>
