---
phase: 12-patient-account
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sanity/schemaTypes/bookingType.ts
  - src/app/api/booking/route.ts
  - src/lib/booking-email.ts
  - sanity.types.ts
autonomous: true
requirements: [ACCT-01, NOTIF-03]

must_haves:
  truths:
    - "New bookings get a managementToken field (UUID) stored on the Sanity document"
    - "Confirmation email contains a single manage URL pointing to /foglalas/:token"
    - "Old /fiokom?cancel= and /fiokom?reschedule= URLs are fully replaced"
    - "Booking schema includes reminderSent boolean for Phase 14 cron idempotency"
  artifacts:
    - path: "src/sanity/schemaTypes/bookingType.ts"
      provides: "managementToken and reminderSent fields"
      contains: "managementToken"
    - path: "src/app/api/booking/route.ts"
      provides: "Token generation on booking creation, manageUrl in email"
      contains: "crypto.randomUUID"
    - path: "src/lib/booking-email.ts"
      provides: "Updated confirmation email with manage link, plus cancellation and reschedule email builders"
      contains: "buildCancellationEmail"
    - path: "sanity.types.ts"
      provides: "Updated TypeScript types with new booking fields"
  key_links:
    - from: "src/app/api/booking/route.ts"
      to: "src/lib/booking-email.ts"
      via: "manageUrl param to buildConfirmationEmail"
      pattern: "manageUrl.*foglalas"
    - from: "src/app/api/booking/route.ts"
      to: "src/sanity/schemaTypes/bookingType.ts"
      via: "managementToken field on create"
      pattern: "managementToken.*randomUUID"
---

<objective>
Add managementToken and reminderSent fields to the booking schema, generate the token on booking creation, update the confirmation email to use a single `/foglalas/:token` manage URL, and create the cancellation + reschedule + reminder email builder functions.

Purpose: Every subsequent plan in Phase 12 depends on the token existing on bookings and the email builders being available. This plan establishes the data contract and email infrastructure.

Output: Updated booking schema with two new fields, updated booking API route generating tokens and passing manage URL, updated confirmation email template, three new email builder functions (cancellation, reschedule, reminder).
</objective>

<execution_context>
@C:/Users/stick/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/stick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-patient-account/12-CONTEXT.md
@.planning/phases/12-patient-account/12-RESEARCH.md
@.planning/phases/11-booking-core/11-01-SUMMARY.md
@.planning/phases/11-booking-core/11-02-SUMMARY.md
@src/sanity/schemaTypes/bookingType.ts
@src/app/api/booking/route.ts
@src/lib/booking-email.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add managementToken and reminderSent fields to booking schema and regenerate types</name>
  <files>src/sanity/schemaTypes/bookingType.ts, sanity.types.ts</files>
  <action>
Add two new fields to the booking Sanity schema in `src/sanity/schemaTypes/bookingType.ts`:

1. `managementToken` field:
   - `name: "managementToken"`
   - `title: "Kezelési token"`
   - `type: "string"`
   - `description: "Egyedi azonosító az időpont kezelési linkjéhez (/foglalas/:token)"`
   - `readOnly: true`
   - Place after the `reservationNumber` field

2. `reminderSent` field:
   - `name: "reminderSent"`
   - `title: "Emlékeztető elküldve"`
   - `type: "boolean"`
   - `description: "Igaz, ha a 24 órás emlékeztető e-mail már ki lett küldve"`
   - `initialValue: false`
   - Place after the `createdAt` field

After adding the fields, run `npx sanity@latest schema extract --enforce-required-fields` then `npx sanity@latest typegen generate` to regenerate `sanity.types.ts` with the new fields. If typegen fails (as it has in prior phases), manually add the fields to the Booking type in `sanity.types.ts`:
- `managementToken?: string;`
- `reminderSent?: boolean;`

Verify with `npx tsc --noEmit` — zero errors.
  </action>
  <verify>
`npx tsc --noEmit` passes. `grep -n "managementToken" src/sanity/schemaTypes/bookingType.ts` shows the field. `grep -n "reminderSent" src/sanity/schemaTypes/bookingType.ts` shows the field.
  </verify>
  <done>bookingType.ts has managementToken (readOnly string) and reminderSent (boolean, default false) fields. sanity.types.ts reflects the new fields. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update booking route to generate token and manage URL, update confirmation email, add 3 new email builders</name>
  <files>src/app/api/booking/route.ts, src/lib/booking-email.ts</files>
  <action>
**Part A — Update `src/app/api/booking/route.ts`:**

1. In the booking creation step (Step 7, the `getWriteClient().create()` call), add `managementToken: crypto.randomUUID()` to the created document. Import `crypto` from Node built-in if needed (in Next.js 15 environment, `crypto.randomUUID()` is available globally).

2. In the `sendConfirmationEmail` helper function:
   - Replace the `cancelUrl` and `rescheduleUrl` construction with a single `manageUrl`:
     ```typescript
     const manageUrl = `${appUrl}/foglalas/${booking.managementToken}`;
     ```
   - Update the `buildConfirmationEmail` call to pass `manageUrl` instead of separate `cancelUrl` and `rescheduleUrl`
   - The `booking` parameter type in `sendConfirmationEmail` must include `managementToken: string`

3. Update the `sendConfirmationEmail` function signature to accept `booking: { _id: string; managementToken: string }` instead of just `{ _id: string }`.

**Part B — Update `src/lib/booking-email.ts`:**

1. **Update `buildConfirmationEmail`** signature:
   - Remove `cancelUrl: string` and `rescheduleUrl: string` parameters
   - Add `manageUrl: string` parameter
   - In the HTML template body, replace the two separate cancel/reschedule buttons with:
     - A single "Időpont kezelése" button linking to `manageUrl` (styled with the existing green button style)
     - Or two buttons both pointing to `manageUrl` but labeled "Lemondás" and "Átütemezés" (both same URL since the management page handles both actions)
   - Keep all existing styling, colors, and Hungarian text patterns

2. **Add `buildCancellationEmail` function:**
   ```typescript
   export function buildCancellationEmail(params: {
     patientName: string;
     serviceName: string;
     date: string;        // Pre-formatted Hungarian date
     time: string;        // "09:20"
     clinicPhone: string;
     clinicAddress: string;
     newBookingUrl: string; // /idopontfoglalas
   }): string
   ```
   - Same HTML structure as confirmation email (navy/pink/green design system, table layout, inline CSS)
   - Subject context: "Időpont lemondva"
   - Body: Greeting, "Az alábbi időpontja lemondásra került:" with service/date/time details
   - CTA button: "Új időpont foglalása" linking to `newBookingUrl`
   - Footer with clinic contact info

3. **Add `buildRescheduleEmail` function:**
   ```typescript
   export function buildRescheduleEmail(params: {
     patientName: string;
     serviceName: string;
     oldDate: string;
     oldTime: string;
     newDate: string;
     newTime: string;
     manageUrl: string;
     clinicPhone: string;
     clinicAddress: string;
   }): string
   ```
   - Same design system
   - Subject context: "Időpont áthelyezve"
   - Body: "Az időpontja áthelyezésre került:" with old date/time crossed out and new date/time highlighted
   - CTA button: "Időpont kezelése" linking to `manageUrl`

4. **Add `buildReminderEmail` function:**
   ```typescript
   export function buildReminderEmail(params: {
     patientName: string;
     serviceName: string;
     date: string;
     time: string;
     clinicPhone: string;
     clinicAddress: string;
   }): string
   ```
   - Same design system
   - Subject context: "Emlékeztető: holnapi időpontja"
   - Body: Friendly reminder with appointment details, no action buttons (24h window passed by reminder time, per CONTEXT.md)
   - Footer with clinic contact info and directions hint

**All email builders MUST use proper Hungarian accented characters (áéíóöőúüű) in ALL user-facing text.** Follow the exact Hungarian text patterns established in the existing `buildConfirmationEmail`.

Verify with `npm run build` — zero errors, zero warnings.
  </action>
  <verify>
`npm run build` passes. `grep -n "manageUrl" src/app/api/booking/route.ts` shows the new URL pattern. `grep -n "buildCancellationEmail\|buildRescheduleEmail\|buildReminderEmail" src/lib/booking-email.ts` shows all three new functions. `grep "fiokom" src/app/api/booking/route.ts src/lib/booking-email.ts` returns no matches (old URLs fully removed).
  </verify>
  <done>Booking route generates managementToken on creation and sends manageUrl to email. Confirmation email uses /foglalas/:token. Three new email builders exist: cancellation, reschedule, reminder. No references to /fiokom remain. Build passes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors
2. `npm run build` — production build passes
3. `grep -rn "fiokom" src/` — no results (old URL pattern fully removed)
4. `grep -n "managementToken" src/sanity/schemaTypes/bookingType.ts` — field exists
5. `grep -n "reminderSent" src/sanity/schemaTypes/bookingType.ts` — field exists
6. `grep -n "buildCancellationEmail\|buildRescheduleEmail\|buildReminderEmail" src/lib/booking-email.ts` — all three exist
7. `grep -n "randomUUID" src/app/api/booking/route.ts` — token generation exists
</verification>

<success_criteria>
- Booking schema has managementToken (readOnly string) and reminderSent (boolean, default false)
- Booking API route generates crypto.randomUUID() token on every new booking
- Confirmation email uses single /foglalas/:token manage URL
- buildCancellationEmail, buildRescheduleEmail, buildReminderEmail functions exist and export from booking-email.ts
- No remaining /fiokom references in codebase
- Production build passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-patient-account/12-01-SUMMARY.md`
</output>
