---
phase: 09-data-foundation-and-gdpr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sanity/schemaTypes/weeklyScheduleType.ts
  - src/sanity/schemaTypes/blockedDateType.ts
  - src/sanity/schemaTypes/serviceType.ts
  - src/sanity/schemaTypes/index.ts
  - src/sanity/desk/structure.ts
  - src/sanity/lib/queries.ts
  - sanity.types.ts
autonomous: true
requirements: [SCHED-01, SCHED-02, SCHED-03, SCHED-04]

must_haves:
  truths:
    - "Doctor can define per-weekday recurring availability (start time, end time, slot duration) in Sanity Studio"
    - "Doctor can toggle a 'day off' checkbox to disable a weekday entirely"
    - "Doctor can add blocked dates in Sanity Studio as an array of date strings"
    - "Each service document has a duration field with preset dropdown (10/15/20/30/45/60 min) defaulting to 20"
    - "Buffer time between appointments is configurable (defaults to 0)"
    - "Schedule and blocked dates appear in a dedicated 'Időpontfoglalás' group in Studio sidebar"
  artifacts:
    - path: "src/sanity/schemaTypes/weeklyScheduleType.ts"
      provides: "Weekly schedule singleton schema with per-weekday config"
      contains: "defineType"
    - path: "src/sanity/schemaTypes/blockedDateType.ts"
      provides: "Blocked dates singleton schema with date array"
      contains: "defineType"
    - path: "src/sanity/schemaTypes/serviceType.ts"
      provides: "Duration field on service documents"
      contains: "appointmentDuration"
    - path: "src/sanity/desk/structure.ts"
      provides: "Időpontfoglalás sidebar group with schedule and blocked dates"
      contains: "Időpontfoglalás"
    - path: "src/sanity/lib/queries.ts"
      provides: "GROQ queries for schedule and blocked dates"
      contains: "weeklyScheduleQuery"
    - path: "sanity.types.ts"
      provides: "TypeScript types for new schemas"
      contains: "WeeklySchedule"
  key_links:
    - from: "src/sanity/schemaTypes/index.ts"
      to: "weeklyScheduleType.ts, blockedDateType.ts"
      via: "import and register in schemaTypes array"
      pattern: "import.*weeklyScheduleType"
    - from: "src/sanity/desk/structure.ts"
      to: "weeklySchedule, blockedDate schemas"
      via: "singleton document items in sidebar"
      pattern: "schemaType.*weeklySchedule"
---

<objective>
Create Sanity schemas for the doctor's weekly schedule configuration and blocked dates, add appointment duration to services, and wire everything into Studio with a dedicated sidebar group.

Purpose: The doctor needs to define when they are available before any booking can happen. These schemas are the data contracts that Phase 11 (slot generation) will read from.
Output: Working Sanity Studio UI where the doctor can configure weekly hours, mark days off, block dates, and set service durations.
</objective>

<execution_context>
@C:/Users/stick/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/stick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-foundation-and-gdpr/09-CONTEXT.md

@src/sanity/schemaTypes/serviceType.ts
@src/sanity/schemaTypes/index.ts
@src/sanity/desk/structure.ts
@src/sanity/lib/queries.ts
@sanity.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create weeklySchedule and blockedDate Sanity schemas</name>
  <files>
    src/sanity/schemaTypes/weeklyScheduleType.ts
    src/sanity/schemaTypes/blockedDateType.ts
    src/sanity/schemaTypes/index.ts
  </files>
  <action>
    Create `weeklyScheduleType.ts` as a singleton document schema using defineType/defineField (same pattern as homepageType).

    Schema name: `weeklySchedule`, title: `Heti beosztás`

    Fields:
    - `defaultSlotDuration` — number field, title "Alapértelmezett időpont hossz (perc)", options list: [10, 15, 20, 30, 45, 60], initialValue: 20. Validation: required.
    - `bufferMinutes` — number field, title "Szünet időpontok között (perc)", initialValue: 0. Description: "Perc szünet két időpont között (0 = nincs szünet)". Validation: min(0).
    - `days` — array of objects, title "Munkanapok", of type inline object with fields:
      - `dayOfWeek` — number, title "Nap", options list with Hungarian day names: [{title: "Hétfő", value: 1}, {title: "Kedd", value: 2}, {title: "Szerda", value: 3}, {title: "Csütörtök", value: 4}, {title: "Péntek", value: 5}, {title: "Szombat", value: 6}, {title: "Vasárnap", value: 0}]. Validation: required.
      - `isDayOff` — boolean, title "Szabadnap", description "Jelölje be, ha ezen a napon nincs rendelés", initialValue: false
      - `startTime` — string, title "Kezdés", description "Formátum: HH:MM (pl. 08:00)". Validation: required when isDayOff is false — use custom validation: `Rule.custom((value, context) => { const parent = context.parent as any; if (!parent?.isDayOff && !value) return 'Kötelező, ha nem szabadnap'; return true; })`.
      - `endTime` — string, title "Befejezés", description "Formátum: HH:MM (pl. 16:00)". Same conditional validation as startTime.

    Array `days` should have initialValue for all 7 days pre-populated (Mon-Sun, with Sat/Sun as isDayOff: true, weekdays isDayOff: false but no times filled). Use validation to ensure exactly 7 items.

    Preview: prepare() returns title "Heti beosztás" (singleton, no dynamic preview needed).

    Create `blockedDateType.ts` as a singleton document schema:

    Schema name: `blockedDate`, title: `Blokkolt napok`

    Fields:
    - `dates` — array of objects, title "Blokkolt dátumok", of type inline object with:
      - `date` — date field, title "Dátum", validation: required
      - `isHoliday` — boolean, title "Ünnepnap", description "Automatikusan hozzáadott magyar ünnepnap", initialValue: false, readOnly: true (only set programmatically)

    No labels/reasons on blocked dates — per CONTEXT.md locked decision: "blocked is blocked".

    Preview: prepare() returns title "Blokkolt napok".

    Update `index.ts`: import both new types and add to schemaTypes array.

    ALL field titles, descriptions, placeholders in Hungarian with proper accented characters.
  </action>
  <verify>
    - `npx biome check src/sanity/schemaTypes/weeklyScheduleType.ts src/sanity/schemaTypes/blockedDateType.ts src/sanity/schemaTypes/index.ts` passes with zero errors
    - `npm run build` passes (TypeScript compilation succeeds)
  </verify>
  <done>
    weeklySchedule singleton schema exists with per-weekday configuration (start/end time, day-off checkbox, slot duration, buffer). blockedDate singleton schema exists with date array. Both registered in schemaTypes index.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add duration field to services, update desk structure, queries, and types</name>
  <files>
    src/sanity/schemaTypes/serviceType.ts
    src/sanity/desk/structure.ts
    src/sanity/lib/queries.ts
    sanity.types.ts
  </files>
  <action>
    **Service duration field:**
    In `serviceType.ts`, add a new field `appointmentDuration` after the existing `order` field:
    - name: `appointmentDuration`
    - title: "Időpont hossza (perc)"
    - type: number
    - description: "Szolgáltatás időtartama percben. Ha hosszabb, mint az alap időpont, több egymást követő slotot foglal le."
    - options: { list: [{title: "10 perc", value: 10}, {title: "15 perc", value: 15}, {title: "20 perc", value: 20}, {title: "30 perc", value: 30}, {title: "45 perc", value: 45}, {title: "60 perc", value: 60}] }
    - initialValue: 20

    **Desk structure update:**
    In `structure.ts`, add a new "Időpontfoglalás" group between "Szolgáltatások" and "Vélemények" (with dividers). The group contains:
    - "Heti beosztás" — singleton: S.document().schemaType("weeklySchedule").documentId("weeklySchedule").title("Heti beosztás szerkesztése")
    - "Blokkolt napok" — singleton: S.document().schemaType("blockedDate").documentId("blockedDate").title("Blokkolt napok szerkesztése")

    Follow existing singleton pattern (like homepage and siteSettings singletons).

    **GROQ queries:**
    In `queries.ts`, add two new queries:

    ```
    // Weekly Schedule
    // Revalidation tag: "weeklySchedule"
    export const weeklyScheduleQuery = defineQuery(`*[_type == "weeklySchedule" && _id == "weeklySchedule"][0]{
      defaultSlotDuration,
      bufferMinutes,
      days[]{
        _key,
        dayOfWeek,
        isDayOff,
        startTime,
        endTime
      }
    }`);

    // Blocked Dates
    // Revalidation tag: "blockedDate"
    export const blockedDatesQuery = defineQuery(`*[_type == "blockedDate" && _id == "blockedDate"][0]{
      dates[]{
        _key,
        date,
        isHoliday
      }
    }`);
    ```

    Also update `allServicesQuery` to include `appointmentDuration` in the projection.

    **TypeScript types:**
    In `sanity.types.ts`, add types for the new schemas:

    ```typescript
    // Weekly schedule day configuration
    export type WeeklyScheduleDay = {
      _key: string;
      dayOfWeek: 0 | 1 | 2 | 3 | 4 | 5 | 6;
      isDayOff: boolean;
      startTime?: string;
      endTime?: string;
    };

    export type WeeklySchedule = {
      _type: "weeklySchedule";
      _id: string;
      defaultSlotDuration: 10 | 15 | 20 | 30 | 45 | 60;
      bufferMinutes: number;
      days: WeeklyScheduleDay[];
    };

    // Blocked date entry
    export type BlockedDateEntry = {
      _key: string;
      date: string;
      isHoliday: boolean;
    };

    export type BlockedDate = {
      _type: "blockedDate";
      _id: string;
      dates: BlockedDateEntry[];
    };
    ```

    Also add `appointmentDuration?: 10 | 15 | 20 | 30 | 45 | 60;` to the existing Service type.

    Follow existing patterns in the codebase for all additions.
  </action>
  <verify>
    - `npx biome check src/sanity/schemaTypes/serviceType.ts src/sanity/desk/structure.ts src/sanity/lib/queries.ts sanity.types.ts` passes with zero errors
    - `npm run build` passes
    - Open Sanity Studio at /studio and verify:
      - "Időpontfoglalás" group appears in sidebar
      - "Heti beosztás" opens with day configuration form
      - "Blokkolt napok" opens with date array
      - Service documents show "Időpont hossza" dropdown
  </verify>
  <done>
    Service documents have appointmentDuration dropdown (10-60 min, default 20). Studio sidebar has Időpontfoglalás group with weeklySchedule and blockedDate singletons. GROQ queries and TypeScript types exist for all new schemas.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `npx biome check .` passes with zero errors (or only pre-existing warnings)
3. Sanity Studio loads at /studio without errors
4. "Időpontfoglalás" group visible in Studio sidebar with two singletons
5. WeeklySchedule form shows 7 day rows with start/end time, day-off checkbox
6. BlockedDate form shows date array (basic — custom input in Plan 03)
7. Service documents show appointmentDuration dropdown with preset values
8. allServicesQuery includes appointmentDuration in response
</verification>

<success_criteria>
- All 4 SCHED requirements have schema-level data contracts in place
- Doctor can open Studio and configure weekly schedule and blocked dates
- Service duration is configurable per-service with preset dropdown
- All new fields use proper Hungarian labels with accented characters
- Build and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-data-foundation-and-gdpr/09-01-SUMMARY.md`
</output>
