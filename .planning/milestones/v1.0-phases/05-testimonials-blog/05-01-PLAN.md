---
phase: 05-testimonials-blog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sanity/schemaTypes/homepageType.ts
  - src/sanity/lib/queries.ts
  - sanity.types.ts
autonomous: true
requirements:
  - TEST-05
  - BLOG-07

must_haves:
  truths:
    - "Homepage schema has testimonialsHeadline and blogHeadline fields editable in Sanity Studio"
    - "GROQ queries exist for latest 3 blog posts, related blog posts by category, and all testimonials"
    - "TypeScript types reflect new homepage fields and blog/testimonial query result shapes"
  artifacts:
    - path: "src/sanity/schemaTypes/homepageType.ts"
      provides: "testimonialsHeadline and blogHeadline fields"
      contains: "testimonialsHeadline"
    - path: "src/sanity/lib/queries.ts"
      provides: "latestBlogPostsQuery, relatedBlogPostsQuery, updated homepageQuery"
      contains: "latestBlogPostsQuery"
    - path: "sanity.types.ts"
      provides: "TestimonialQueryResult, BlogPostQueryResult, BlogPostDetailResult types"
      contains: "TestimonialQueryResult"
  key_links:
    - from: "src/sanity/lib/queries.ts"
      to: "sanity.types.ts"
      via: "query result types match GROQ projection shapes"
      pattern: "BlogPostQueryResult|TestimonialQueryResult"
---

<objective>
Add homepage schema fields for testimonial and blog section headings, create new GROQ queries for latest blog posts and related posts, and update TypeScript types to support the Phase 5 UI components.

Purpose: All Phase 5 UI components need typed data shapes and queries before they can be built. This plan establishes the data layer foundation.
Output: Updated homepageType schema, new/updated GROQ queries, and query result types in sanity.types.ts.
</objective>

<execution_context>
@C:/Users/stick/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/stick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-testimonials-blog/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add testimonial and blog section fields to homepage schema and update homepage GROQ query</name>
  <files>src/sanity/schemaTypes/homepageType.ts, src/sanity/lib/queries.ts</files>
  <action>
    1. In `src/sanity/schemaTypes/homepageType.ts`, add two new fields AFTER the `labTestsSubtitle` field and BEFORE the CTA section:

    ```
    // Testimonials section
    defineField({
      name: "testimonialsHeadline",
      title: "Vélemények cím",
      type: "string",
    }),

    // Blog section
    defineField({
      name: "blogHeadline",
      title: "Blog cím",
      type: "string",
    }),
    ```

    2. In `src/sanity/lib/queries.ts`, update `homepageQuery` to include `testimonialsHeadline` and `blogHeadline` in the projection (add them after `labTestsSubtitle`).

    3. Add a new query `latestBlogPostsQuery` — fetches the 3 most recent blog posts ordered by publishedAt desc, projecting: _id, title, slug, featuredImage, excerpt. NO category field (CONTEXT.md: no category tags on cards). Revalidation tag comment: "blogPost".

    4. Add a new query `relatedBlogPostsQuery` — fetches up to 3 blog posts in the same category, excluding the current post. Accepts params `$categoryId` and `$currentPostId`. Projection: _id, title, slug, featuredImage, excerpt. Fallback note in comment: "If category is null, the caller should fall back to latestBlogPostsQuery". Revalidation tag comment: "blogPost".

    Follow existing query patterns: use `defineQuery()`, add section comment headers with revalidation tag notes, keep consistent code style with existing queries.
  </action>
  <verify>Run `npx biome check .` — zero errors. Run `npx tsc --noEmit` — zero type errors.</verify>
  <done>homepageType has testimonialsHeadline and blogHeadline fields; queries.ts has latestBlogPostsQuery and relatedBlogPostsQuery; existing queries unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add query result types for testimonials and blog posts to sanity.types.ts</name>
  <files>sanity.types.ts</files>
  <action>
    Add three new query result types at the bottom of the "Query Result Types" section in `sanity.types.ts`:

    1. `TestimonialQueryResult` — matches `allTestimonialsQuery` projection shape:
    ```typescript
    export type TestimonialQueryResult = {
      _id: string;
      patientName?: string;
      photo?: SanityImageObject;
      text?: string;
      condition?: string;
      order?: number;
    };
    ```

    2. `BlogPostQueryResult` — matches `latestBlogPostsQuery` projection shape (NO category, per CONTEXT.md):
    ```typescript
    export type BlogPostQueryResult = {
      _id: string;
      title?: string;
      slug?: Slug;
      featuredImage?: SanityImageObject;
      excerpt?: string;
    };
    ```

    3. `BlogPostDetailResult` — matches `blogPostBySlugQuery` projection shape (already exists in queries.ts):
    ```typescript
    export type BlogPostDetailResult = {
      _id: string;
      title?: string;
      slug?: Slug;
      category?: { _id: string; name?: string; slug?: Slug };
      featuredImage?: SanityImageObject;
      body?: Array<PortableTextBlock | BlogPostBodyImage>;
      excerpt?: string;
      metaDescription?: string;
      ogImage?: SanityImageObject;
      publishedAt?: string;
    };
    ```

    Also update the `Homepage` type to include the new fields:
    - `testimonialsHeadline?: string;`
    - `blogHeadline?: string;`

    These go after `labTestsSubtitle` and before `ctaHeadline`.
  </action>
  <verify>Run `npx tsc --noEmit` — zero type errors. Run `npx biome check .` — zero errors.</verify>
  <done>sanity.types.ts has TestimonialQueryResult, BlogPostQueryResult, BlogPostDetailResult types; Homepage type includes testimonialsHeadline and blogHeadline fields.</done>
</task>

</tasks>

<verification>
- `npx biome check .` exits 0 with no errors
- `npx tsc --noEmit` exits 0 with no type errors
- `npm run build` completes without errors
- homepageQuery includes testimonialsHeadline and blogHeadline
- latestBlogPostsQuery and relatedBlogPostsQuery exist in queries.ts
- All 3 new query result types exist in sanity.types.ts
</verification>

<success_criteria>
The data layer is ready for Phase 5 UI components: homepage schema fields for section headings, GROQ queries for testimonial and blog data, and TypeScript types for all query results.
</success_criteria>

<output>
After completion, create `.planning/phases/05-testimonials-blog/05-01-SUMMARY.md`
</output>
