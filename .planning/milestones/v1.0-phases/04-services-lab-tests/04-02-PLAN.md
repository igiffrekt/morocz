---
phase: 04-services-lab-tests
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/LabTestsSection.tsx
autonomous: true
requirements:
  - LAB-01
  - LAB-02
  - LAB-03
  - LAB-04
  - LAB-05

must_haves:
  truths:
    - "Lab test section has a dark navy background with rounded corners"
    - "Lab test cards show test name, description, and fixed price in Ft"
    - "Cards cycle through pastel colors: peach, mint, cream"
    - "Cards fade/slide in on scroll"
    - "A 'További vizsgálatok' link is visible at the bottom"
    - "All lab test data comes from props (Sanity-driven)"
  artifacts:
    - path: "src/components/sections/LabTestsSection.tsx"
      provides: "Lab tests section with dark navy background and pastel cards"
      contains: "bg-primary"
  key_links:
    - from: "src/components/sections/LabTestsSection.tsx"
      to: "motion/react"
      via: "Scroll-triggered entrance animation"
      pattern: "whileInView"
---

<objective>
Build the LabTestsSection component: a dark navy background section with pastel-colored lab test cards showing name, description, and fixed price, plus a "További vizsgálatok" link. Cards animate in on scroll.

Purpose: Lab test pricing is a key differentiator for the practice — the colorful pastel cards on dark navy create visual warmth and draw attention to affordable pricing.

Output: `src/components/sections/LabTestsSection.tsx` — a client component receiving lab tests and heading as props.
</objective>

<execution_context>
@C:/Users/stick/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/stick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-services-lab-tests/04-CONTEXT.md
@src/sanity/lib/queries.ts
@src/app/globals.css
@sanity.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build LabTestsSection component with dark navy background and pastel cards</name>
  <files>src/components/sections/LabTestsSection.tsx</files>
  <action>
Create `src/components/sections/LabTestsSection.tsx` as a `"use client"` component (needed for scroll-triggered animations via `whileInView`).

**Props interface:**

```typescript
interface LabTestData {
  _id: string;
  name?: string;
  description?: string;
  price?: number;
  illustration?: SanityImageObject;
  order?: number;
}

interface LabTestsSectionProps {
  heading?: string;
  labTests: LabTestData[];
}
```

Note: Per the CONTEXT.md locked decision, lab test cards show ONLY the fixed price (no discount, no originalPrice, no "from" prefix). The `price` field alone is used, formatted as "8 000 Ft" with thousand separator space.

**Section wrapper:**
- `<section className="bg-primary rounded-3xl px-6 py-12 md:px-10 md:py-16">` — full dark navy background with rounded corners matching site style

**Section heading:**
- `<h2 className="text-3xl md:text-4xl font-extrabold text-white mb-8">` — white text on dark navy
- Single heading only, no subtitle (per decision)

**Lab test cards grid:**
- Grid layout: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4` (Claude's discretion on columns — 3 on desktop works well with the card content density)
- Each card is a `<motion.div>` with scroll-triggered entrance:
  - `initial={{ opacity: 0, y: 20 }}`
  - `whileInView={{ opacity: 1, y: 0 }}`
  - `viewport={{ once: true, margin: "-50px" }}`
  - `transition={{ duration: 0.5, ease: "easeOut", delay: index * 0.1 }}` (staggered by index, capped at 0.4s max delay = `Math.min(index * 0.1, 0.4)`)

**Pastel card colors (cycling):**
- Define array: `const pastelColors = ["bg-[#ffebe4]", "bg-[#edf8f3]", "bg-[#fdf8eb]"]` (peach, mint, cream)
- Apply via `pastelColors[index % pastelColors.length]`

**Individual card styling:**
- `rounded-2xl p-6 flex flex-col gap-3 min-h-[160px]`
- Apply the cycling pastel background color class
- Test name: `<h3 className="text-lg font-bold text-primary">` — dark navy text on pastel
- Description: `<p className="text-sm text-gray-700 line-clamp-3">` — readable gray text
- Price: `<p className="text-xl font-extrabold text-primary mt-auto">` — pushed to bottom with mt-auto
- Format price with Hungarian thousand separator space: create a helper function `formatPrice(price: number): string` that returns e.g. "8 000 Ft" using `price.toLocaleString("hu-HU")` + " Ft"
- If illustration exists, optionally render with `next/image` via `urlFor` at small size (48x48 displayed). Position at top-right of card as a decorative element: `absolute top-4 right-4 opacity-60`. Make the card `relative overflow-hidden` to contain it.

**"További vizsgálatok" link:**
- Below the card grid, centered
- Styled as a pill link/button: `inline-flex items-center gap-2 bg-white/10 hover:bg-white/20 text-white font-semibold rounded-full px-6 py-3 transition-colors duration-300`
- Include a right arrow SVG icon (chevron right)
- Use `<a href="#" ...>` for now (actual link target TBD — href will be updated when lab tests page exists)
- Wrap in `<div className="mt-8 text-center">`

**Import requirements:**
- `"use client"` directive at top
- `import { motion } from "motion/react"`
- `import Image from "next/image"` (for illustration rendering)
- `import { urlFor } from "@/sanity/lib/image"`
- `import type { SanityImageObject } from "../../../sanity.types"`

**Biome compliance:**
- Named export: `export function LabTestsSection`
- All images via `next/image`
- No lint suppressions needed

Run verification: `npx biome check src/components/sections/LabTestsSection.tsx`
  </action>
  <verify>
1. `npx biome check src/components/sections/LabTestsSection.tsx` passes with no errors
2. `npx tsc --noEmit` passes
3. File exists and exports `LabTestsSection` function
  </verify>
  <done>
LabTestsSection.tsx exists as a client component with:
- Dark navy (`bg-primary`) background with rounded corners
- Lab test cards cycling through peach/mint/cream pastel backgrounds
- Each card showing: test name, description, fixed price formatted as "X XXX Ft"
- Cards animate in on scroll with staggered fade+slide
- "További vizsgálatok" link at bottom in white-on-navy style
- All data from props (no Sanity calls in component)
- Biome and TypeScript pass
  </done>
</task>

</tasks>

<verification>
1. `npx biome check .` — zero errors
2. `npx tsc --noEmit` — zero type errors
3. `npm run build` — production build passes
</verification>

<success_criteria>
- LabTestsSection renders dark navy section with rounded corners
- Pastel cards (peach, mint, cream) cycle through colors
- Cards show name, description, and price only (no discount, no original price)
- Price formatted with thousand separator: "8 000 Ft"
- Cards fade+slide in on scroll via whileInView
- "További vizsgálatok" link visible at bottom
- Biome and TypeScript pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-services-lab-tests/04-02-SUMMARY.md`
</output>
