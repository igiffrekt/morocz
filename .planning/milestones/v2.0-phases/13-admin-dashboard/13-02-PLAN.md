---
phase: 13-admin-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/app/admin/page.tsx
  - src/components/admin/AdminDashboard.tsx
  - src/components/admin/AdminCalendar.tsx
  - src/components/admin/AdminWeekView.tsx
  - src/components/admin/AdminDayPanel.tsx
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03]

must_haves:
  truths:
    - "Admin sees a two-panel dashboard: calendar on the left, day appointments on the right"
    - "Admin can switch between month and week calendar views"
    - "Month view shows colored dots on days with bookings"
    - "Week view shows vertical timeline with appointment blocks (Google Calendar style)"
    - "Day panel shows appointments in chronological order with time, patient name, service, phone, and status badge"
    - "Clicking a day in month view loads that day's appointments in the right panel"
    - "Dashboard loads with today selected by default"
  artifacts:
    - path: "src/app/admin/page.tsx"
      provides: "Server Component fetching session + initial today bookings, renders AdminDashboard"
      contains: "AdminDashboard"
    - path: "src/components/admin/AdminDashboard.tsx"
      provides: "Top-level client shell with state management (selectedDate, viewMode, bookings)"
      contains: "use client"
    - path: "src/components/admin/AdminCalendar.tsx"
      provides: "Month calendar grid with booking dots and month/week toggle"
      contains: "AdminCalendar"
    - path: "src/components/admin/AdminWeekView.tsx"
      provides: "Vertical timeline week view with positioned appointment blocks"
      contains: "AdminWeekView"
    - path: "src/components/admin/AdminDayPanel.tsx"
      provides: "Right panel listing day's appointments with status badges"
      contains: "AdminDayPanel"
  key_links:
    - from: "src/components/admin/AdminDashboard.tsx"
      to: "/api/admin/bookings"
      via: "fetch on date range change"
      pattern: "fetch.*api/admin/bookings"
    - from: "src/app/admin/page.tsx"
      to: "src/components/admin/AdminDashboard.tsx"
      via: "Server Component renders client dashboard with initial data"
      pattern: "AdminDashboard.*initialBookings"
    - from: "src/components/admin/AdminCalendar.tsx"
      to: "src/components/admin/AdminDashboard.tsx"
      via: "onSelectDate callback"
      pattern: "onSelectDate"
---

<objective>
Build the admin dashboard UI: replace the placeholder /admin page with a two-panel layout (calendar left, day appointments right), month/week toggle, and day appointment list. This is the primary visual surface of the admin area.

Purpose: Gives the admin a functional daily view of appointments and calendar navigation. The patient detail modal and cancel flow are in Plan 03.
Output: Working admin dashboard with calendar and day panel, fetching data from Plan 01's API route.
</objective>

<execution_context>
@C:/Users/stick/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/stick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-admin-dashboard/13-CONTEXT.md
@.planning/phases/13-admin-dashboard/13-RESEARCH.md
@.planning/phases/13-admin-dashboard/13-01-SUMMARY.md
@src/app/admin/page.tsx
@src/app/admin/layout.tsx
@src/components/admin/AdminLogin.tsx
@src/components/booking/Step2DateTime.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace admin page placeholder with AdminDashboard shell, AdminCalendar, and AdminDayPanel</name>
  <files>
    src/app/admin/page.tsx
    src/components/admin/AdminDashboard.tsx
    src/components/admin/AdminCalendar.tsx
    src/components/admin/AdminDayPanel.tsx
  </files>
  <action>
**src/app/admin/page.tsx** — Update the admin section (session confirmed as admin):
- Keep the existing no-session → `<AdminLogin />` and wrong-role → access-denied blocks exactly as-is
- Replace the placeholder dashboard div with: fetch today's bookings server-side via `getWriteClient().fetch()` using the same GROQ as GET /api/admin/bookings (inline the query, not an API call — Server Component has direct access). Pass `todayBookings` and `session` as props to `<AdminDashboard />`.
- Also fetch bookings for the current month (startDate = first of month, endDate = last of month) for calendar dots. Pass as `initialMonthBookings`.
- Keep `<AdminSignOut />` — position it in the dashboard header area (pass as part of session/header props or render in AdminDashboard)

**src/components/admin/AdminDashboard.tsx** — "use client" top-level shell:
- State: `selectedDate` (string YYYY-MM-DD, default today), `viewMode` ("month" | "week"), `dayBookings` (array, initialized from props), `monthBookings` (array, initialized from props), `isLoading` (boolean)
- On selectedDate change: fetch bookings for that single day from GET /api/admin/bookings (startDate=endDate=selectedDate) → update dayBookings
- On month navigation: fetch bookings for the new month range → update monthBookings (for calendar dots)
- Layout: flex row with gap. Left panel ~60% width: calendar. Right panel ~40% width: day appointments list.
- Dark theme inline styles consistent with admin layout (#0f172a bg, #1e293b cards, #f8fafc text, #94a3b8 muted text, #64748b secondary)
- Header bar: "Admin felület" title on left, admin name from session, AdminSignOut button on right
- Pass `onSelectDate` callback to AdminCalendar, `onBookingClick` callback to AdminDayPanel (Plan 03 will wire the modal)

**src/components/admin/AdminCalendar.tsx** — "use client" calendar component:
- Month/Week toggle buttons at top (styled as pill-shaped toggle, dark theme)
- Props: `selectedDate`, `onSelectDate`, `monthBookings` (for dots), `viewMode`, `onViewModeChange`, `onMonthChange` (fires when user navigates months), `onBookingClick` (for week view appointment block clicks)
- When viewMode === "month": render month grid (reuse calendar logic from Step2DateTime.tsx — Monday-first, HU_WEEKDAYS ["H", "K", "Sze", "Cs", "P", "Szo", "V"], month/year navigation arrows)
  - Group monthBookings by slotDate client-side into a Map<string, number>
  - Show colored dot(s) on days that have bookings (use #99CEB7 accent green dot)
  - Selected day highlighted with accent color background
  - Clicking ANY day (not just ones with bookings) calls `onSelectDate(dateStr)`
  - Month/year header with left/right arrow navigation, fires `onMonthChange` with new year/month
  - No 30-day max limit — admin can browse any date freely
- When viewMode === "week": render `<AdminWeekView />` (separate component, Task 2)

**src/components/admin/AdminDayPanel.tsx** — "use client" right panel:
- Props: `bookings` (array for selected day), `selectedDate` (string), `isLoading`, `onBookingClick` (fires with booking object when row clicked)
- Header: formatted Hungarian date of selected day using `new Date(selectedDate).toLocaleDateString("hu-HU", { year: "numeric", month: "long", day: "numeric", weekday: "long" })`
- Each appointment row: time (slotTime), patient name, service name, phone number, status badge
- Status badges: "Visszaigazolva" (green bg) / "Lemondva" (red/muted bg) — inline styled
- Rows in chronological order (already sorted by API)
- Clickable rows — entire row is a button/clickable div, calls `onBookingClick(booking)` (Plan 03 wires the modal)
- Empty state: "Ezen a napon nincs foglalás." message centered, muted text
- Loading state: simple "Betöltés..." text or a subtle spinner
- All styling: dark theme with inline styles or dark-appropriate Tailwind classes matching admin palette (#1e293b card bg, #f8fafc text, #94a3b8 muted)
  </action>
  <verify>
Run `npx tsc --noEmit` — zero TypeScript errors.
Run `npx biome check src/app/admin/ src/components/admin/` — zero lint errors.
Run `npm run build` — build succeeds.
  </verify>
  <done>
Admin page renders two-panel dashboard with calendar (month view with booking dots, month navigation) on the left and day appointment list on the right. Today selected by default. Clicking a day fetches and displays that day's bookings. All styled in dark admin theme.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build AdminWeekView vertical timeline component</name>
  <files>src/components/admin/AdminWeekView.tsx</files>
  <action>
Create the Google Calendar-style vertical timeline week view.

**src/components/admin/AdminWeekView.tsx** — "use client":
- Props: `selectedDate` (string — determines which week to show), `bookings` (all bookings for the displayed week, fetched by parent), `onSelectDate` (clicking a day column header selects that day), `onBookingClick` (clicking an appointment block opens detail modal)
- Calculate the Monday-to-Sunday range for the week containing selectedDate
- Display: CSS Grid layout
  - Fixed first column: hour labels from 07:00 to 18:00 (11 hours) — left gutter
  - 7 day columns: Mon-Sun
  - Day headers at top: show abbreviated Hungarian day name + date number (e.g., "H 23", "K 24"). Highlight today's column header. Clicking header calls `onSelectDate`.
  - Hour rows: horizontal lines at each hour mark for visual grid
  - Appointment blocks: absolutely positioned within their day column
    - Top position: `timeToTopPercent(slotTime)` — minutes from 07:00 as percentage of total 07:00-18:00 range
    - Height: `durationToHeightPercent(appointmentDuration || 20)` — based on service duration in minutes
    - Background: #99CEB7 (accent green) for confirmed, #475569 (muted slate) for cancelled
    - Content: patient name + time range (e.g., "09:00 - 09:20") — truncate if block is too small
    - Clickable: calls `onBookingClick(booking)` on click
    - Rounded corners, subtle shadow for depth
  - Helper functions (inline in file):
    ```typescript
    const DAY_START_HOUR = 7;
    const DAY_END_HOUR = 18;
    const TOTAL_MINUTES = (DAY_END_HOUR - DAY_START_HOUR) * 60;

    function timeToTopPercent(time: string): number {
      const [h, m] = time.split(":").map(Number);
      const minutesFromStart = (h ?? 0) * 60 + (m ?? 0) - DAY_START_HOUR * 60;
      return (minutesFromStart / TOTAL_MINUTES) * 100;
    }

    function durationToHeightPercent(durationMinutes: number): number {
      return (durationMinutes / TOTAL_MINUTES) * 100;
    }
    ```
  - Group bookings by slotDate → Map<string, Booking[]>
  - For each day column, position blocks based on that day's bookings
- Week navigation: AdminDashboard handles week changes when viewMode is "week" — when selectedDate changes, the week view shows the corresponding week. The parent re-fetches the week range bookings.
- Scrollable container if content overflows vertically (min-height for the timeline area)
- Dark theme consistent with other admin components
  </action>
  <verify>
Run `npx tsc --noEmit` — zero TypeScript errors.
Run `npx biome check src/components/admin/AdminWeekView.tsx` — zero lint errors.
Run `npm run build` — build succeeds.
  </verify>
  <done>
AdminWeekView renders a vertical timeline with hour labels on the left, 7 day columns with appointment blocks positioned by time, color-coded by status. Blocks are clickable. Week is derived from the selected date.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. /admin page renders the two-panel dashboard layout (not the old placeholder)
4. Month calendar shows current month with navigation arrows, highlights selected day, shows booking dots
5. Month/Week toggle switches between month grid and vertical timeline
6. Day panel shows the selected day's bookings in chronological order with status badges
7. Clicking a day in calendar updates the day panel
8. Week view shows appointment blocks positioned by time with correct colors
9. All text is in Hungarian with proper accented characters
10. Dark admin theme is consistent across all components
</verification>

<success_criteria>
- Two-panel layout: calendar left (~60%), day panel right (~40%)
- Month view with booking dots and month navigation
- Week view with vertical timeline and positioned appointment blocks
- Day panel with chronological booking list and status badges
- Today selected by default on page load
- Fetches real data from GET /api/admin/bookings
- All Hungarian UI strings with proper accented characters
- Dark admin theme matching existing admin layout
</success_criteria>

<output>
After completion, create `.planning/phases/13-admin-dashboard/13-02-SUMMARY.md`
</output>
