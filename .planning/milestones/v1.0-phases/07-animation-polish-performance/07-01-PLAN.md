---
phase: 07-animation-polish-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/motion/FadeIn.tsx
  - src/components/motion/StaggerChildren.tsx
  - src/components/sections/HeroSection.tsx
  - src/components/sections/HeroServiceCards.tsx
  - src/components/sections/ServicesSection.tsx
  - src/components/sections/LabTestsSection.tsx
  - src/components/sections/TestimonialsSection.tsx
  - src/components/sections/BlogSection.tsx
  - src/components/layout/Footer.tsx
autonomous: true
requirements:
  - ANIM-03
  - ANIM-04
  - RESP-02

must_haves:
  truths:
    - "Scrolling down triggers visible fade-up entrance animations on every major section"
    - "Card groups (hero cards, services, lab tests, blog) stagger in one card at a time"
    - "Animations play only once per section — scrolling back up and down does not replay them"
    - "On mobile, animations are simpler (shorter duration, reduced stagger) to avoid jank"
    - "Content above the fold (hero) still animates on mount, not on scroll"
  artifacts:
    - path: "src/components/motion/FadeIn.tsx"
      provides: "Scroll-triggered fade animation wrapper"
      contains: "whileInView"
    - path: "src/components/motion/StaggerChildren.tsx"
      provides: "Scroll-triggered stagger container"
      contains: "whileInView"
  key_links:
    - from: "src/components/sections/ServicesSection.tsx"
      to: "src/components/motion/FadeIn.tsx"
      via: "FadeIn wrapping section heading"
      pattern: "FadeIn.*viewport"
    - from: "src/components/sections/HeroServiceCards.tsx"
      to: "whileInView"
      via: "motion.section viewport trigger"
      pattern: "whileInView"
---

<objective>
Convert all section animations from mount-based (`animate`) to scroll-triggered (`whileInView`) with staggered card entrances, and ensure mobile degradation.

Purpose: Sections currently animate on page load, meaning below-fold content animates invisibly. Switching to scroll-triggered animations creates a "reveal on scroll" experience that rewards scrolling and matches the design reference.

Output: All 7 section components animate in when scrolled into view. Card groups stagger. Mobile uses simplified timing.
</objective>

<execution_context>
@C:/Users/stick/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/stick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-animation-polish-performance/07-CONTEXT.md
@src/components/motion/FadeIn.tsx
@src/components/motion/StaggerChildren.tsx
@src/components/motion/MotionProvider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor FadeIn and StaggerChildren for scroll-triggered animations</name>
  <files>
    src/components/motion/FadeIn.tsx
    src/components/motion/StaggerChildren.tsx
  </files>
  <action>
**FadeIn.tsx — add `viewport` prop support:**

Add an optional `viewport` prop (boolean, default `false`). When `viewport` is true:
- Replace `animate` with `whileInView` on the motion.div
- Add `viewport={{ once: true, amount: 0.2 }}` (plays once, triggers at 20% visibility)
- Keep `initial` the same

When `viewport` is false (default), keep existing `animate` behavior for backward compatibility (hero section still needs mount animations).

The interface becomes:
```tsx
interface FadeInProps extends HTMLMotionProps<"div"> {
  direction?: "up" | "down" | "left" | "right" | "none";
  delay?: number;
  duration?: number;
  viewport?: boolean;
  children: ReactNode;
}
```

Implementation: conditionally spread either `animate={...}` or `whileInView={...}` based on the `viewport` prop. Example:
```tsx
const animationProps = viewport
  ? { whileInView: { opacity: 1, x: 0, y: 0 }, viewport: { once: true, amount: 0.2 } }
  : { animate: { opacity: 1, x: 0, y: 0 } };
```

**StaggerChildren.tsx — add `viewport` prop support:**

Same pattern. Add optional `viewport` prop (boolean, default `false`). When true:
- Replace `animate="visible"` with `whileInView="visible"`
- Add `viewport={{ once: true, amount: 0.2 }}`

When false, keep existing `animate="visible"` for backward compatibility.

**IMPORTANT:** Import `motion` from `motion/react` (already done). Do NOT import `useInView` — use the declarative `whileInView` prop instead, which is the idiomatic Motion v12 pattern.

**IMPORTANT:** `MotionConfig reducedMotion="user"` is already at root — this automatically disables whileInView animations when OS prefers reduced motion. No additional reduced-motion handling needed in these components.
  </action>
  <verify>
Run `npx biome check src/components/motion/` — zero errors. Run `npm run build` — builds without type errors.
  </verify>
  <done>FadeIn and StaggerChildren both support `viewport` prop. When viewport=true, they trigger on scroll. When viewport=false (default), they trigger on mount. Backward compatible — no existing usage breaks.</done>
</task>

<task type="auto">
  <name>Task 2: Add scroll-triggered animations to all section components</name>
  <files>
    src/components/sections/HeroSection.tsx
    src/components/sections/HeroServiceCards.tsx
    src/components/sections/ServicesSection.tsx
    src/components/sections/LabTestsSection.tsx
    src/components/sections/TestimonialsSection.tsx
    src/components/sections/BlogSection.tsx
    src/components/layout/Footer.tsx
  </files>
  <action>
Update each section component per the CONTEXT.md animation table. The general pattern is:

**Section headings/text blocks** — wrap in `<FadeIn viewport>` (fade-up as a unit).
**Card grids** — wrap in `<StaggerChildren viewport>` with individual card items using stagger variants.

Per-component changes:

**HeroSection.tsx** — NO CHANGES to animation triggers. Hero is above the fold and must animate on mount (current `animate` behavior). Keep existing FadeIn usage without `viewport`. Keep badge `motion.div` with `animate`. This section is the one exception — it plays on page load, not on scroll.

**HeroServiceCards.tsx** — Convert from `animate="visible"` to `whileInView="visible"` with `viewport={{ once: true, amount: 0.2 }}` on the `motion.section` container. Remove the `delayChildren: 0.6` (no delay needed for scroll trigger — the section is only visible when scrolled to). Keep `staggerChildren: 0.1`. Keep individual card `variants={item}` unchanged.

**ServicesSection.tsx** — Wrap the section heading and filter tabs in `<FadeIn viewport>`. The service cards already use AnimatePresence for filter switching — keep that as-is, but wrap the overall grid container in a `<FadeIn viewport>` so the whole card area fades in on first scroll. The filter interaction animations are separate from the entrance animation.

**LabTestsSection.tsx** — Wrap the section heading in `<FadeIn viewport>`. Wrap the card grid area in `<FadeIn viewport delay={0.15}>` so cards fade in slightly after heading.

**TestimonialsSection.tsx** — Wrap the section heading in `<FadeIn viewport>`. Wrap the testimonial card/carousel area in `<FadeIn viewport delay={0.15}>`.

**BlogSection.tsx** — Wrap the header row (heading + link) in `<FadeIn viewport>`. Wrap each blog card individually: left card in `<FadeIn viewport delay={0}>`, right card in `<FadeIn viewport delay={0.15}>` for a subtle stagger effect.

**Footer.tsx** — Footer is a Server Component (no 'use client'). Add 'use client' directive to Footer OR wrap footer content in a new client wrapper. Simpler: convert Footer to a client component by adding 'use client' at top, then wrap the main footer content in `<FadeIn viewport>`. Import FadeIn from `@/components/motion/FadeIn`.

**Mobile degradation (RESP-02):** The timing values above (0.5s duration, 0.1s stagger, easeOut) are already lightweight. For explicit mobile degradation: on HeroServiceCards, reduce stagger from 0.1 to 0.08 on mobile. This can be done by detecting viewport width with a `useMediaQuery` hook OR simply using the lower 0.08s stagger universally (CONTEXT.md says max 0.08s per item — use 0.08s as default stagger in HeroServiceCards). Update the container variants `staggerChildren: 0.08`.

**Animation timing (from CONTEXT.md):**
- Fade-up duration: 0.5s
- Stagger delay between cards: 0.08s (max per CONTEXT.md)
- Ease: "easeOut"
- Viewport trigger threshold: 20% (`amount: 0.2`)
- Play once: `once: true`
  </action>
  <verify>
Run `npm run build` — builds without errors. Run `npx biome check src/components/` — zero errors. Start dev server (`npm run dev`) and scroll the homepage — each section should animate in as it enters the viewport.
  </verify>
  <done>All 6 below-fold sections (HeroServiceCards, Services, LabTests, Testimonials, Blog, Footer) animate in on scroll with whileInView. Hero retains mount animation. Card groups use 0.08s stagger. Animations play once only. Reduced motion automatically handled by MotionConfig.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `npx biome check .` passes with zero errors
3. Dev server scroll test: each section fades in when scrolled into view
4. Hero section still animates on page load (not scroll-triggered)
5. Scrolling back up does not replay any section animation
6. HeroServiceCards stagger at 0.08s per card
</verification>

<success_criteria>
- Every below-fold section has visible scroll-triggered entrance animation
- Card groups stagger in individually
- Hero section retains mount-based animation (above the fold)
- Animations play once per page visit
- Build passes, Biome passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-animation-polish-performance/07-01-SUMMARY.md`
</output>
