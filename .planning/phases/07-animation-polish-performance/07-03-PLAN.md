---
phase: 07-animation-polish-performance
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - src/components/sections/HeroSection.tsx
  - src/components/sections/HeroServiceCards.tsx
  - src/components/sections/BlogSection.tsx
  - src/components/blog/RelatedPosts.tsx
autonomous: false
requirements:
  - ANIM-05
  - ANIM-07
  - RESP-02

must_haves:
  truths:
    - "Blog post links and lab test links use CircleWipeLink for page transitions"
    - "All CTA buttons show arrow slide/expand animation on hover"
    - "With OS reduced motion enabled, all animations are skipped and all content is immediately visible"
    - "Mobile PageSpeed Insights scores LCP < 2.5s, CLS < 0.1"
  artifacts:
    - path: "src/components/sections/BlogSection.tsx"
      provides: "Blog cards wrapped in CircleWipeLink"
      contains: "CircleWipeLink"
    - path: "src/components/sections/HeroServiceCards.tsx"
      provides: "Card button hover animation"
      contains: "hover"
  key_links:
    - from: "src/components/sections/BlogSection.tsx"
      to: "src/components/motion/CircleWipeLink.tsx"
      via: "import and usage on blog card links"
      pattern: "CircleWipeLink"
---

<objective>
Wire CircleWipeLink into content navigation, polish button hover animations, and verify reduced motion + Core Web Vitals across the full animation stack.

Purpose: The circle wipe and button hover polish are the final animation features. This plan also serves as the integration verification — ensuring all Phase 7 animations work together without breaking performance.

Output: Circle wipe on content navigation, polished button hovers, verified CWV scores, verified reduced motion behavior.
</objective>

<execution_context>
@C:/Users/stick/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/stick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-animation-polish-performance/07-CONTEXT.md
@.planning/phases/07-animation-polish-performance/07-01-SUMMARY.md
@.planning/phases/07-animation-polish-performance/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire CircleWipeLink into content links and polish button hovers</name>
  <files>
    src/components/sections/BlogSection.tsx
    src/components/blog/RelatedPosts.tsx
    src/components/sections/HeroServiceCards.tsx
    src/components/sections/HeroSection.tsx
  </files>
  <action>
**Wire CircleWipeLink into blog navigation:**

In `BlogSection.tsx`: Replace the `<Link>` wrapping each blog card with `<CircleWipeLink>`. Import from `@/components/motion/CircleWipeLink`. The href pattern is `/blog/${post.slug?.current}` — this matches the CircleWipeLink trigger pattern (`/blog/`).

In `RelatedPosts.tsx`: Same change — replace `<Link>` wrapping related post cards with `<CircleWipeLink>`.

Note: Lab test detail links (`/laborvizsgalatok/[slug]`) — check if any section component currently links to individual lab test pages. If LabTestsSection has card links to detail pages, wrap those too. If lab test cards don't link to detail pages (they might just be display cards), no change needed.

**Button hover polish (ANIM-05):**

Review existing button hover animations:

1. **HeroSection CTA** — already has sophisticated arrow slide/expand hover animation (group-hover arrow scale, pill translate). This is DONE per planning context. No changes needed.

2. **HeroServiceCards arrow buttons** — already have hover animations (group-hover:scale-110, group-hover:rotate-[45deg], white fill expanding from center). These are good. Verify they feel responsive and smooth. If the timing feels right, no changes needed.

3. **ServicesSection** — check if service cards have hover states. If they only have layout animation for filtering, that's fine — no additional hover animation needed per CONTEXT.md.

4. **General CTA buttons** — the compact header CTA already has hover styling. Footer links don't need hover animations.

**Audit for any un-animated buttons that should have animation per ANIM-05:** The requirement says "arrow icon slide/expand, background transitions". Currently HeroSection CTA and HeroServiceCards buttons already have this. If no other buttons with arrows exist, ANIM-05 is fully covered.

**If any button hover animation needs refinement:**
- Ensure transition duration is 300-500ms
- Use `ease-out` or `cubic-bezier(0.25, 0.46, 0.45, 0.94)` for natural feel
- Arrow icons should translate-x by ~4px on hover for the "slide" effect
  </action>
  <verify>
Run `npm run build` — builds without errors. Run `npx biome check .` — zero errors. Start dev server, click a blog card — circle wipe should play before navigation. Check that blog detail page loads after wipe. Hover over HeroSection CTA and HeroServiceCards buttons — arrow animations should feel smooth.
  </verify>
  <done>CircleWipeLink wired into all blog card links (BlogSection + RelatedPosts). Button hover animations audited and polished. All internal content navigation uses circle wipe transition.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 7 animations + performance check</name>
  <action>
Human verifies the complete Phase 7 animation stack:
1. Intro typewriter overlay (first visit) with slide-up reveal
2. Short fade intro on repeat visits
3. Scroll-triggered section entrance animations on all below-fold sections
4. Staggered card entrances on HeroServiceCards, Services, LabTests, Blog
5. Circle wipe transition on blog post navigation
6. Button hover animations on CTA buttons
7. Reduced motion: all animations disabled when OS preference is set

**Test 1 — Intro sequence (first visit):**
1. Open http://localhost:3000 in an incognito/private window
2. EXPECTED: Dark navy overlay appears, logo icon fades in, "Dr. Morocz Angela" types out character by character, then overlay slides up revealing page content
3. Total duration should feel like ~2 seconds (snappy, not slow)

**Test 2 — Intro sequence (repeat visit):**
1. In the same tab, reload the page (Ctrl+R)
2. EXPECTED: Quick fade-in/fade-out (~0.5s), no typewriter, straight to content

**Test 3 — Scroll animations:**
1. After page loads, scroll down slowly through the entire homepage
2. EXPECTED: Each section animates in as you scroll to it:
   - HeroServiceCards: cards stagger in from bottom
   - Services: heading fades up, then cards visible
   - Lab Tests: heading fades up, cards fade in
   - Testimonials: heading fades up, carousel fades in
   - Blog: heading fades up, cards stagger in
   - Footer: content fades up
3. Scroll back up — animations should NOT replay

**Test 4 — Circle wipe transition:**
1. Click a blog post card on the homepage
2. EXPECTED: Pink circle wipe animation plays (~1s), then blog post page loads
3. Navigate back, click another blog link — same effect

**Test 5 — Reduced motion:**
1. In browser devtools (Chrome): Rendering tab → check "Emulate CSS media feature prefers-reduced-motion"
2. Reload page in incognito
3. EXPECTED: No typewriter animation — logo and name appear instantly, quick fade to content. Scrolling shows content immediately (no scroll animations). Clicking blog link navigates immediately (no circle wipe).

**Test 6 — Mobile viewport:**
1. Open Chrome devtools, toggle device toolbar (iPhone 14 or similar)
2. Reload incognito
3. EXPECTED: Intro plays (slightly simpler feel), scroll animations work but feel quick, no jank or stuttering

**Test 7 — Performance (quick check):**
1. Run Lighthouse on http://localhost:3000 (Mobile, Performance category)
2. Check: LCP < 2.5s, CLS < 0.1, INP green
3. Note: Intro overlay should NOT inflate LCP because page content renders behind it
  </action>
  <verify>Type "approved" if all tests pass, or describe any issues you see with specific test numbers.</verify>
  <done>User approves all 7 animation features working correctly — intro, scroll reveals, circle wipe, button hovers, reduced motion, mobile behavior, and Core Web Vitals passing.</done>
</task>

</tasks>

<verification>
1. CircleWipeLink used on all blog card navigation
2. Button hover animations smooth on all CTA buttons
3. Full animation pipeline verified visually (intro → scroll → transitions)
4. Reduced motion fully disables all animations
5. Core Web Vitals in passing range on mobile
6. `npm run build` and `npx biome check .` both pass
</verification>

<success_criteria>
- All 7 ANIM requirements (ANIM-01 through ANIM-07) verified working
- RESP-02 (mobile animation degradation) confirmed
- LCP < 2.5s, CLS < 0.1 on mobile Lighthouse
- User approves visual quality of all animations
</success_criteria>

<output>
After completion, create `.planning/phases/07-animation-polish-performance/07-03-SUMMARY.md`
</output>
