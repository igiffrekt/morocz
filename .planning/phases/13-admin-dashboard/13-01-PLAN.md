---
phase: 13-admin-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/admin/bookings/route.ts
  - src/app/api/admin/booking-cancel/route.ts
  - src/lib/booking-email.ts
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04]

must_haves:
  truths:
    - "GET /api/admin/bookings returns bookings for a date range, sorted by slotTime, with patient details"
    - "POST /api/admin/booking-cancel cancels a booking by _id with admin session auth, enforces 24h rule, sends admin cancellation email"
    - "buildAdminCancellationEmail generates Hungarian email stating clinic cancelled the appointment with optional reason"
  artifacts:
    - path: "src/app/api/admin/bookings/route.ts"
      provides: "Admin bookings query API (GET with date range + optional single-date mode)"
      exports: ["GET"]
    - path: "src/app/api/admin/booking-cancel/route.ts"
      provides: "Admin cancel API with session-based auth"
      exports: ["POST"]
    - path: "src/lib/booking-email.ts"
      provides: "buildAdminCancellationEmail function"
      contains: "buildAdminCancellationEmail"
  key_links:
    - from: "src/app/api/admin/bookings/route.ts"
      to: "auth.api.getSession"
      via: "session role check"
      pattern: "session\\.user\\.role.*admin"
    - from: "src/app/api/admin/booking-cancel/route.ts"
      to: "src/lib/booking-email.ts"
      via: "buildAdminCancellationEmail import"
      pattern: "buildAdminCancellationEmail"
    - from: "src/app/api/admin/booking-cancel/route.ts"
      to: "getWriteClient"
      via: "Sanity patch for cancel + slot release"
      pattern: "getWriteClient.*patch"
---

<objective>
Build the admin API layer for Phase 13: a GET endpoint for fetching bookings by date range (powers both calendar dots and day panel), a POST endpoint for admin-initiated cancellation (session-authed, not token-authed), and the admin cancellation email template.

Purpose: All dashboard UI plans depend on these API endpoints. Separating API from UI keeps each plan focused and within context budget.
Output: Two API routes and one email builder function ready for Plan 02/03 to consume.
</objective>

<execution_context>
@C:/Users/stick/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/stick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-admin-dashboard/13-CONTEXT.md
@.planning/phases/13-admin-dashboard/13-RESEARCH.md
@src/app/api/booking-cancel/route.ts
@src/lib/booking-email.ts
@src/lib/email.ts
@src/lib/auth.ts
@src/lib/sanity-write-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GET /api/admin/bookings and POST /api/admin/booking-cancel routes</name>
  <files>
    src/app/api/admin/bookings/route.ts
    src/app/api/admin/booking-cancel/route.ts
  </files>
  <action>
Create two admin API routes. Both must start with admin session verification:

```typescript
const session = await auth.api.getSession({ headers: request.headers });
if (!session) return Response.json({ error: "Bejelentkezés szükséges." }, { status: 401 });
if (session.user.role !== "admin") return Response.json({ error: "Jogosulatlan hozzáférés." }, { status: 403 });
```

**GET /api/admin/bookings:**
- Accept query params: `startDate` and `endDate` (both YYYY-MM-DD strings)
- Validate both params exist and are valid date strings
- Use `getWriteClient()` (not CDN client — admin data must be real-time accurate per RESEARCH.md pitfall 2)
- GROQ query: `*[_type == "booking" && slotDate >= $startDate && slotDate <= $endDate] | order(slotDate asc, slotTime asc) { _id, patientName, patientEmail, patientPhone, reservationNumber, service->{name, appointmentDuration}, slotDate, slotTime, status, managementToken }`
- Include ALL statuses (confirmed + cancelled) — day panel shows status badges
- Return: `{ bookings: [...] }`
- Add `export const dynamic = "force-dynamic";`

**POST /api/admin/booking-cancel:**
- Zod schema: `{ bookingId: z.string().min(1), reason: z.string().optional() }`
- Fetch booking by `_id` (NOT by managementToken — admin uses _id directly): `*[_type == "booking" && _id == $bookingId][0]{ _id, patientName, patientEmail, patientPhone, reservationNumber, service->{name}, slotDate, slotTime, status }`
- Use `$bookingId` as the GROQ param name (not `$token` which is reserved in @sanity/client)
- If booking not found → 404
- If booking.status !== "confirmed" → 400 "Ez az időpont már nem aktív."
- Enforce 24h rule (same as patient cancel per locked CONTEXT.md decision): copy `isWithin24Hours` helper from `src/app/api/booking-cancel/route.ts`. If within 24h → 403 "Az időpont már nem mondható le (kevesebb mint 24 óra van hátra)."
- Patch booking status to "cancelled" via `getWriteClient().patch(booking._id).set({ status: "cancelled" }).commit()`
- Release slotLock (same pattern as booking-cancel/route.ts): `slotLock-{slotDate}-{slotTime with colon replaced by dash}`, patch to available + unset bookingRef, wrap in try/catch
- Fire-and-forget `buildAdminCancellationEmail` send via `sendEmail()` — include reason if provided
- Return: `{ success: true, message: "Az időpont sikeresen lemondva." }`
  </action>
  <verify>
Run `npx tsc --noEmit` — zero TypeScript errors.
Run `npx biome check src/app/api/admin/` — zero lint errors.
Run `npm run build` — build succeeds with no errors.
  </verify>
  <done>
GET /api/admin/bookings returns bookings by date range with admin session auth. POST /api/admin/booking-cancel cancels by _id with session auth, 24h enforcement, slot release, and admin cancel email. Both routes reject non-admin sessions with 401/403.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add buildAdminCancellationEmail to booking-email.ts</name>
  <files>src/lib/booking-email.ts</files>
  <action>
Add a new exported function `buildAdminCancellationEmail` to `src/lib/booking-email.ts`. Follow the existing email builder pattern (inline CSS, table layout, shared colour constants navy/pink/green/lightGrey/textDark/textMuted).

Signature:
```typescript
export function buildAdminCancellationEmail(params: {
  patientName: string;
  serviceName: string;
  reservationNumber: string;
  date: string;    // Pre-formatted Hungarian date string
  time: string;    // "09:20"
  reason?: string; // Optional admin-provided reason
  clinicPhone: string;
  clinicAddress: string;
  newBookingUrl: string;
}): string
```

Key differences from `buildCancellationEmail` (patient self-cancel):
1. Subject line: "Időpont lemondva a rendelő által — Mórocz Medical"
2. Header: "A rendelő lemondta az Ön időpontját" (NOT "Időpont lemondva")
3. Conditional reason block: if `reason` is provided, render a paragraph: "Indoklás: {reason}" styled with a light grey background block
4. Softer CTA: "Új időpont foglalása" link to `newBookingUrl` but not as a large prominent button — use a text link or smaller button (clinic cancelled, not patient's choice)
5. Footer: same clinic contact info pattern with phone and address as existing emails
6. Include "Útvonal" button linking to Google Maps `mapsUrl` (same as other email templates)
7. Include the reservation number in the email body (same pattern as existing emails)

Use the existing `mapsUrl` constant already in the file. Reuse the shared colour variables.
  </action>
  <verify>
Run `npx tsc --noEmit` — zero TypeScript errors.
Run `npx biome check src/lib/booking-email.ts` — zero lint errors.
Visually inspect function: accepts optional reason param, renders different header text than patient cancel email, conditional reason block.
  </verify>
  <done>
buildAdminCancellationEmail exists in booking-email.ts with distinct "A rendelő lemondta" messaging, optional reason field, and softer CTA — clearly distinguishable from patient self-cancel email template.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (zero errors)
2. `npm run build` succeeds
3. `npx biome check src/app/api/admin/ src/lib/booking-email.ts` passes
4. GET /api/admin/bookings requires admin session (returns 401/403 for non-admin)
5. POST /api/admin/booking-cancel requires admin session, validates bookingId, enforces 24h rule, patches booking, releases slot, sends email
6. buildAdminCancellationEmail is exported and has distinct header/subject from patient cancel email
</verification>

<success_criteria>
- Two admin API routes exist and compile
- Both routes authenticate via `auth.api.getSession()` with admin role check
- Cancel route enforces 24h rule per locked CONTEXT.md decision
- Admin cancellation email template has distinct messaging ("A rendelő lemondta")
- Email includes optional reason field
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/13-admin-dashboard/13-01-SUMMARY.md`
</output>
