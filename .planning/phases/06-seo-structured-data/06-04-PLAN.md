---
phase: 06-seo-structured-data
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/adatkezelesi-tajekoztato/page.tsx
  - src/components/ui/CookieNotice.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - SEO-06
  - SEO-07
  - SEO-09

must_haves:
  truths:
    - "A privacy policy page is accessible at /adatkezelesi-tajekoztato with the same header/footer as the rest of the site"
    - "The privacy policy page renders Portable Text content from Sanity"
    - "A cookie notice appears as a small corner toast for first-time visitors"
    - "The cookie notice can be dismissed and stays dismissed via localStorage"
    - "The cookie notice links to the privacy policy page"
    - "All cookie notice and privacy policy text is in Hungarian"
  artifacts:
    - path: "src/app/adatkezelesi-tajekoztato/page.tsx"
      provides: "Privacy policy page with Portable Text rendering"
      contains: "privacyPolicyQuery"
    - path: "src/components/ui/CookieNotice.tsx"
      provides: "Cookie consent notice component"
      contains: "localStorage"
    - path: "src/app/layout.tsx"
      provides: "Layout with CookieNotice rendered"
      contains: "CookieNotice"
  key_links:
    - from: "src/app/adatkezelesi-tajekoztato/page.tsx"
      to: "src/sanity/lib/queries.ts"
      via: "sanityFetch with privacyPolicyQuery"
      pattern: "privacyPolicyQuery"
    - from: "src/components/ui/CookieNotice.tsx"
      to: "/adatkezelesi-tajekoztato"
      via: "Link href"
      pattern: "adatkezelesi-tajekoztato"
    - from: "src/app/layout.tsx"
      to: "src/components/ui/CookieNotice.tsx"
      via: "import and render"
      pattern: "CookieNotice"
---

<objective>
Create the privacy policy page and cookie notice component.

Purpose: GDPR compliance for Hungarian visitors — a privacy policy page editable from Sanity, and an informational cookie notice that links to it.
Output: Privacy policy page at /adatkezelesi-tajekoztato, cookie notice corner toast in layout, both fully in Hungarian.
</objective>

<execution_context>
@C:/Users/stick/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/stick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-seo-structured-data/06-CONTEXT.md
@.planning/phases/06-seo-structured-data/06-01-SUMMARY.md
@src/app/layout.tsx
@src/components/blog/PortableTextRenderer.tsx
@src/sanity/lib/queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create privacy policy page at /adatkezelesi-tajekoztato</name>
  <files>src/app/adatkezelesi-tajekoztato/page.tsx</files>
  <action>
    Create an async Server Component page that:

    1. Fetches privacy policy data using sanityFetch with privacyPolicyQuery (tag: "privacyPolicy")
    2. Exports generateMetadata() returning:
       - title: `policy?.title ?? "Adatkezelesi tajekoztato"` (will use layout template suffix)
       - description: "Adatkezelesi tajekoztato — Morocz Medical"
       - robots: { index: true, follow: true }
    3. Renders the page content:
       - A `<main>` with centered max-width content (max-w-3xl mx-auto px-4 py-8 — same pattern as blog detail)
       - `<h1>` with the policy title (or fallback "Adatkezelesi tajekoztato")
       - If `lastUpdated` exists, show "Utolso frissites: {formatted date}" in small text
       - Portable Text body rendered using the existing PortableTextRenderer component
       - If no policy document exists in Sanity, show a placeholder message: "Az adatkezelesi tajekoztato hamarosan elerheto lesz." (The privacy policy will be available soon.)

    Import PortableTextRenderer from "@/components/blog/PortableTextRenderer".
    Import type PrivacyPolicy from sanity.types.
    Use the same page structure (padding, max-width, heading styles) as the blog detail page for visual consistency.

    The page inherits the header/footer from layout.tsx automatically (same layout as the rest of the site per CONTEXT.md decision).

    All text in Hungarian. Use proper Hungarian diacriticals in the rendered text (the variable names/paths stay ASCII).
  </action>
  <verify>
    Run `npm run build` to confirm the route compiles. Navigate to http://localhost:3000/adatkezelesi-tajekoztato in the browser (dev server) to confirm it renders with header and footer.
  </verify>
  <done>
    The page at /adatkezelesi-tajekoztato renders Portable Text content from the Sanity privacyPolicy singleton, or a placeholder if no content exists yet. It shares the site's header/footer layout. All text is in Hungarian.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create cookie notice component and wire into layout</name>
  <files>
    src/components/ui/CookieNotice.tsx
    src/app/layout.tsx
  </files>
  <action>
    **CookieNotice.tsx** — Create a client component ('use client'):

    1. On mount, check `localStorage.getItem("cookie-notice-dismissed")`.
    2. If already dismissed, render nothing (return null).
    3. If not dismissed, render a small corner toast card in the bottom-right corner:
       - Position: `fixed bottom-4 right-4 z-50`
       - Size: small card, ~320px max-width
       - Background: white with shadow-lg, rounded-2xl, p-4
       - Content:
         - Small heading: "Sutik hasznalata" (Cookie usage) — text-sm font-bold text-primary
         - Body text: "Ez a weboldal sutiket hasznal a jobb felhasznaloi elmeny erdekeben. Bovebb informaciot az adatkezelesi tajekoztatoban talal." (This website uses cookies for a better user experience. More information can be found in our privacy policy.)
         - A link "Adatkezelesi tajekoztato" pointing to `/adatkezelesi-tajekoztato` — styled as text-accent underline
         - A dismiss button: "Rendben" (OK) — bg-primary text-white rounded-xl px-4 py-2 text-sm
       - Design: navy/white/accent palette matching site design system per CONTEXT.md
       - Add `role="dialog"` and `aria-label="Cookie tajekoztatas"` for accessibility

    4. On dismiss button click:
       - `localStorage.setItem("cookie-notice-dismissed", "true")`
       - Hide the notice (set state to dismissed)

    5. Use `useState` for visibility. Use `useEffect` for initial localStorage check (must be client-side only — localStorage is not available during SSR).

    6. Add a fade-in animation using motion/react: `initial={{ opacity: 0, y: 20 }}`, `animate={{ opacity: 1, y: 0 }}`, `exit={{ opacity: 0, y: 20 }}`.

    All displayed text in Hungarian with proper diacriticals.

    **layout.tsx** — Import CookieNotice and render it inside the MotionProvider, after the page content (after the `</div>` container but still inside `<body>`):
    ```tsx
    <body>
      <MotionProvider>
        <div className="max-w-[88rem] mx-auto ...">
          ...
        </div>
        <CookieNotice />
      </MotionProvider>
    </body>
    ```

    The CookieNotice is position:fixed so it doesn't affect layout flow. It sits outside the max-width container. Since CookieNotice is a client component and MotionProvider is already a client boundary, this works fine.
  </action>
  <verify>
    Run `npm run build` to confirm the layout compiles with CookieNotice. Run `npx biome check .` for lint compliance. In the browser, verify: cookie notice appears on first visit, clicking "Rendben" dismisses it, refreshing page does not show it again (localStorage persists).
  </verify>
  <done>
    Cookie notice renders as a bottom-right corner toast on first visit. It shows Hungarian text, links to the privacy policy page, and can be permanently dismissed via localStorage. The notice is non-blocking (informational only). It matches the site design system. Build passes.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes without errors
- `npx biome check .` reports zero errors
- /adatkezelesi-tajekoztato route exists and renders with header/footer
- Cookie notice appears on first page load, dismisses on click, stays dismissed on refresh
- Cookie notice links to /adatkezelesi-tajekoztato
- All text is in Hungarian
</verification>

<success_criteria>
The Hungarian privacy policy page is accessible at /adatkezelesi-tajekoztato, renders Portable Text from Sanity, and shares the site layout. A cookie notice toast appears in the bottom-right corner for first-time visitors, links to the privacy policy, and can be permanently dismissed. Both features are GDPR-compliant for Hungarian visitors.
</success_criteria>

<output>
After completion, create `.planning/phases/06-seo-structured-data/06-04-SUMMARY.md`
</output>
